# 玉兆 开发待办（TODO）

> 当前基线：v2.1.0（自 2.0.2 之后所有未发布改动合并）。冒烟 542 项全绿。
> 本文件记录已完成评审、待开发的增强功能。开发顺序与范围以评审结论为准。

## 评审结论摘要（2026-08-29）

**候选池评审**：可交互设计是唯一改变产品形态的候选，其余内容型候选（玉牌扩组/舆图名录/坊市求购区）依赖交互基座才有完整体感；外观主题与 TTS 传音砍掉或无限期搁置。

**双玉兆架构定案**：
- 玩家 = 安装插件的 Tavo 用户；玉兆数据以游戏角色为主体（玩家"偷窥"角色视角）。
- 玩家交互需独立「玩家域」，与「角色域」数据隔离但可互通。
- 玩家传讯不立即回复，在后续 Tavo 对话轮次中自然回复（不联动生成流）。
- UI 一套视图 + 域切换按钮；私人数据随域切换，公开数据跨域复用。

---

## 一、交互基座第一层（角色玉兆只读 + 检索筛选）✅ 已完成

- [x] 只读浏览：复用详情页/Modal 基础设施，纯内存过滤，不做 CRUD
- [x] 检索筛选（聊天内关键词过滤 + 列表页过滤）
- 范围约束：只读，不改数据；为后续所有玩家交互打底

## 二、双域架构 + 传讯通道（一期核心）✅ 已完成

- [x] 玩家域 / 角色域数据隔离：
  - 玩家域与角色域同构但无模型域字段（不经评估、无 revision/指纹/processedTurns）
  - 玩家域存储：宿主变量（chat 作用域）+ 本地镜像 + 全局备份三层；不进世界书
- [x] 传讯通道（唯一跨域写入点）：
  - 玩家传讯 → 角色域 `yz-player` 联系人未读行（幂等：传讯 id；已读游标）
  - 未读行计入 9000 预算，优先级高于归档行（四级淘汰末位）；每轮未读上限 5 条 + 摘要行
  - 后续轮次模型自然回信（回信 = 角色域普通协议数据），玩家域只读展示
  - 游标推进（注入即已读）；UI 显示 已送达/已读/已回 标记
- [x] 域切换按钮：切换数据源而非 UI；私有数据随域切换；公开数据（论坛/坊市行情拍卖）跨域一致并带标识
- [x] 玩家域一期只读视图：本命玉牌（空态专属文案）、芥子空间、交流讯息（传讯会话）、记事玉册、坊市订单、舆图
- [x] 一期验收项（待真机回归）：传讯在后续轮次被自然回复；玩家域只读不污染角色域；公开数据跨域一致；预算内注入不挤掉角色主线数据

## 三、玩家域 CRUD（二期）✅ 已完成

- [x] 记事玉册 / 芥子空间编辑（玩家直写，不经评估）：
  - 玉册夹/备忘增删改（备忘含禁制锁定；删除玉册夹级联其下备忘）
  - 物品/钱财增删改（货币种类为唯一键，重命名即替换）
- [x] 坊市订单操作：创建/编辑/删除，方向归一 buy/sell
- 交互：列表页新建 CTA + 行尾编辑；表单页保存/两击确认删除；角色域列表保持只读

## 四、角色域内容扩展（可排序，验证「协议行扩展审计」成本）

- [ ] 玉牌扩组（功法/羁绊）——先做第一个以摸清扩展成本
- [ ] 舆图地点名录（map 分区扩展，配合世界书召回）
- [ ] 坊市求购区（market 扩展）

## 五、公开数据玩家身份发布（三期）

- [ ] forum 行 owner 维度（玩家身份发帖；评估矩阵、世界书关键词同步扩展）

---

## 已否决 / 搁置

- 外观主题（纯装饰，无玩法增益）
- TTS 传音（无音频权限，宿主能力未验证，技术可行性存疑）

## 开发约束（每个候选必过）

所有新增数据形态必须逐项过「数据形态扩展审计」：
buildCurrent 窗口化、MAX_BASELINE_CHARS 预算、diff 合并、达标评估、内容指纹去重、
版本迁移、四层备份链、世界书归档关键词——任何一项遗漏都会破坏现有 542 项测试基线语义。
玩家域新增数据形态另需过：三层存储（不进世界书）、不经模型评估、跨域幂等、CRUD 校验与删除级联。

- 冒烟门禁：`node --check entry.js && node tests/smoke.mjs`（基线 542 项全绿）
- 发布门禁：MCP `tavo_plugin_validate` → `tavo_plugin_audit` → `tavo_plugin_package`，产物随版本号更新出包
