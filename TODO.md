# 玉兆 开发待办（TODO）

> 当前基线：v2.2.0（已发布，冒烟 701 项全绿）。
> 本文件记录已完成评审、待开发的增强功能。开发顺序与范围以评审结论为准。

## 评审结论摘要（2026-08-29）

**候选池评审**：可交互设计是唯一改变产品形态的候选，其余内容型候选（玉牌扩组/舆图名录/坊市求购区）依赖交互基座才有完整体感；外观主题与 TTS 传音砍掉或无限期搁置。

**双玉兆架构定案**：
- 玩家 = 安装插件的 Tavo 用户；玉兆数据以游戏角色为主体（玩家"偷窥"角色视角）。
- 玩家交互需独立「玩家域」，与「角色域」数据隔离但可互通。
- 玩家传讯不立即回复，在后续 Tavo 对话轮次中自然回复（不联动生成流）。
- UI 一套视图 + 域切换按钮；私人数据随域切换，公开数据跨域复用。

---

## 一、交互基座第一层（角色玉兆只读 + 检索筛选）✅ 已完成

- [x] 只读浏览：复用详情页/Modal 基础设施，纯内存过滤，不做 CRUD
- [x] 检索筛选（聊天内关键词过滤 + 列表页过滤）
- 范围约束：只读，不改数据；为后续所有玩家交互打底

## 二、双域架构 + 传讯通道（一期核心）✅ 已完成

- [x] 玩家域 / 角色域数据隔离：
  - 玩家域与角色域同构但无模型域字段（不经评估、无 revision/指纹/processedTurns）
  - 玩家域存储：宿主变量（chat 作用域）+ 本地镜像 + 全局备份三层；不进世界书
- [x] 传讯通道（唯一跨域写入点）：
  - 玩家传讯 → 角色域 `yz-player` 联系人未读行（幂等：传讯 id；已读游标）
  - 未读行计入 9000 预算，淘汰优先级最高（五级淘汰末位）；每轮未读上限 5 条 + 摘要行
  - 后续轮次模型自然回信（回信 = 角色域普通协议数据），玩家域只读展示
  - 游标推进（注入即已读）；UI 显示 已送达/已读/已回 标记
  - 评审加固：镜像双向保尾 20 + id 全量去重；diffChats 对 yz-player 代码级只读防护
    （可 +msg self 回复，不可删/伪造/改写玩家消息）；镜像 await 后状态同一性复查
- [x] 域切换按钮：切换数据源而非 UI；私有数据随域切换；公开数据（论坛/坊市行情拍卖/群聊）跨域一致并带标识
- [x] 域默认与可辨性：按钮文案 = 当前域（角色域/玩家域，避免目标域文案误导用户以为默认在玩家域）；
  每次打开玉兆默认回到角色域；玩家域渲染绝不回退角色域数据（数据域隔离）
- [x] 玩家域一期只读视图：本命玉牌（空态专属文案）、芥子空间、交流讯息（传讯会话）、记事玉册、坊市订单、舆图
- [x] 一期验收项（待真机回归）：传讯在后续轮次被自然回复；玩家域只读不污染角色域；公开数据跨域一致；预算内注入不挤掉角色主线数据

## 三、玩家域 CRUD（二期）✅ 已完成

- [x] 记事玉册 / 芥子空间编辑（玩家直写，不经评估）：
  - 玉册夹/备忘增删改（备忘含禁制锁定；删除玉册夹级联其下备忘）
  - 物品/钱财增删改（货币种类为唯一键，重命名即替换）
- [x] 坊市订单操作：创建/编辑/删除，方向归一 buy/sell
- 交互：列表页新建 CTA + 行尾编辑；表单页保存/两击确认删除；角色域列表保持只读

## 四、角色域内容扩展（可排序，验证「协议行扩展审计」成本）

- [x] 玉牌扩组（功法/羁绊）——六组：基本/仪容/修为/功法/羁绊/隐秘
  - 组名别名归一（功法/心法/法诀/technique…、羁绊/缘分/bond…）；canonical 键
    功法名/羁绊对象去重；达标底线：功法、羁绊各至少 1 条；issue 双语回显；
    引导行/约束文本同步扩展；旧档经 issue 回声用 + 行自动补齐，无需版本迁移
  - 扩展成本结论：纯增量（groups 为通用 {id, fields} 结构），八个审计维度全部零改造
- [x] 舆图地点名录（map 分区扩展，配合世界书召回）：
  - `place｜id｜地点名｜所属域｜说明` 行（别名 地点/地名）；`map.places` 上限 20
  - 达标底线：地点名录至少 2 处；删到 2 处以下被达标门禁拦截
  - 基线窗口 `RECENT_PLACE_ROWS=6`：窗口外只给 archived 摘要行
  - 世界书召回：窗口外地点进「玉兆·地点名录」关键词条目（正文提及地点名注入完整名录），
    封印舆图后条目移除；与基线窗口共用切分、不重叠不遗漏
- [x] 坊市求购区（market 扩展）：
  - `request｜id｜物品名｜品阶｜描述｜出价｜求购人` 行（别名 求购/求购单），上限 12
  - 达标底线：求购至少 1 条；删空求购区被达标门禁拦截（issue 沿用 market.rows）
  - 基线窗口 `RECENT_REQUEST_ROWS=6`：窗口外只给 archived 摘要行
  - 求购为公开数据（与行情/拍卖同源），玩家域带「公开」标识；订单仍为私有数据
  - 坊市页新增「求购」tab（行情/求购/拍卖/订单）

## 五、公开数据玩家身份发布（三期）✅ 已完成

- [x] forum 行 owner 维度（玩家身份发帖）：
  - `post` 行尾可选 owner 字段（player = 玩家真实发帖，缺省 = 角色发帖）
  - 玩家发帖源数据在玩家域（`fp-<n>` 确定性 id，三层存储），`syncPlayerPosts`
    幂等镜像进角色域论坛成为公开数据，作者名 = `{{user}}`（persona.name，缺省回退 catalog）
  - 保护三层：diff 门禁（模型 +post/-post 不得触碰 owner=player 行）、提示词明令、
    镜像对账（全量轮改写/删失后按玩家域还原，含被剥落的 owner 标记；角色侧评论保留）
  - 评估矩阵扩展：玩家帖子不参与帖子数/评论数底线（与传讯豁免同语义），
    不拉低达标、不凑人数
  - 世界书关键词扩展：玩家帖子永远全行注入基线（drop:false + last 仅极端让位）、
    不进窗口归档、不进世界书——玩家内容绝不进角色记忆
  - 玩家域论坛分区只承载玩家帖子（归一化过滤 owner=player）
  - UI：玩家域论坛发帖 CTA + 行尾编辑 + 「玩家」身份标记（双域一致）；角色域无 CRUD 控件
  - 评审加固：对账按 mine id 集排除剥落 owner 的行（消灭同 id 重复帖）；玩家帖 id 撞
    角色帖时双方同步改名 fp-<n> 找空位（角色帖保留，幂等）；镜像 await 后状态同一性复查

---

## 已否决 / 搁置

- 外观主题（纯装饰，无玩法增益）
- TTS 传音（无音频权限，宿主能力未验证，技术可行性存疑）

## 开发约束（每个候选必过）

所有新增数据形态必须逐项过「数据形态扩展审计」：
buildCurrent 窗口化、MAX_BASELINE_CHARS 预算、diff 合并、达标评估、内容指纹去重、
版本迁移、四层备份链、世界书归档关键词——任何一项遗漏都会破坏现有 701 项测试基线语义。
玩家域新增数据形态另需过：三层存储（不进世界书）、不经模型评估、跨域幂等、CRUD 校验与删除级联；公开数据发布另需过：owner 维度、镜像对账（含评论保留与 id 撞车改名）、评估豁免、提示词保护、跨域镜像并发复查（await 后状态同一性）。

- 冒烟门禁：`node --check entry.js && node tests/smoke.mjs`（基线 701 项全绿）
- 发布门禁：MCP `tavo_plugin_validate` → `tavo_plugin_audit` → `tavo_plugin_package`，产物随版本号更新出包
