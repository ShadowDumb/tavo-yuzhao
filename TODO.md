# 玉兆 开发待办（TODO）

> 本文件只记录未完成工作。已完成工作见 CHANGELOG.md。

## 待办项

- [ ] 玉牌(乾)「暂无刻录」空态：确认默认空间是否需预置演示字段。
- [ ] 真实 DOM 与交互回归：验证联系人/群聊删除、搜索输入、二次确认、撤销、表单提交与焦点恢复。
- [ ] 移动端视口与无障碍回归：验证窄屏适配、软键盘遮挡、触屏手势与屏幕阅读器标签。
- [ ] 异常与边界场景模拟：验证存储配额超限、跨标签页并发冲突、重载后异步生成结果处理。

## 真机视觉走查记录

> 设备：OPPO OPD2401（Android 16，序列 900af430），横屏 3000×2120。宿主 `app.bitbear.tav`，插件 `com.shadowdumb.yu-zhao` v3.0.0。走查 8 卦扇区 + 太极枢纽。

### 交互与布局观察

- **固高弹窗导致的少数据页留白**：`.yz-window` 固定高度为 `88vh`（`max-height: 860px`），设计上为保持八卦主盘与子页面间切换时不发生高度跳动；但当进入数据较少页面（如 `天下舆图` 仅 2 处名山、`芥子空间` 仅 4 件法宝）时，卡片仅占上半部，下半部出现约 1/3 高度的背景空白。
- **表单弹窗键盘避让（多字段滚动截断）**：所有实体表单通用同套模态卡片（`max-height: 85vh; overflow-y: auto`）。「新建 钱财」仅 2 字段（矮卡片）在键盘弹起后能完整居中；而「新增玉牌属性」含 3 字段（高卡片）在软键盘弹起后高度超出剩余可视视口，触发模态内部纵向滚动，焦点在输入框时底部的「取消」和「保存」按钮被推移至卡片下边缘之外发生半截断。
- **无障碍语义缺失**：玉兆由 Flutter 宿主内嵌 `android.webkit.WebView` 渲染。未开启 TalkBack 服务时 Chromium 不向系统生成交互式节点树；且子页面按钮、卡片、Tab 等大多缺少 `role="button"`、`tabindex="0"` 及 `aria-label` 语义标签。

## 远期规划

- [ ] 天下论坛跨玩家社区功能。

## 开发门禁

- 冒烟门禁：`node scripts/build.mjs --check && node --check entry.js && node tests/smoke.mjs`
- 发布门禁：MCP `tavo_plugin_validate` → `tavo_plugin_audit` → `tavo_plugin_package`
