# 玉兆 · 修仙法器设计文档

## 一、概念与命名

- **玉**：温润通灵之物，修真者随身法器的常见载体。
- **兆**：占卜之兆、先机之兆，与《周易》八卦同源。
- **玉兆** ＝ "以玉通灵、见微知著"。名字本身就绑定了八卦/占卜意象，因此主界面用**太极八卦**排布在主题上是自洽的，不是硬套。
- 定位：修仙世界的"手机"，但所有功能、文案、视觉、数据协议全部用修真文明包装。

## 二、整体架构（沿用本仓库插件模式）

协议驱动：

```
generation:prepare  注入提示词（只同步"已启用"的功能）
        ↓
模型正文正常结束 → 末尾输出 <yz_jade> 协议块
        ↓
generation:success  解析 → 校验 → 剥离正文协议块 → 渲染
```

模块分层：

| 模块 | 职责 |
| --- | --- |
| Core | 数据消毒、逐功能校验、状态机、去重 |
| Protocol | 解析 `<yz_*>` 标签 |
| I18n | 中/英双语包 |
| Runtime | 按 chatId 隔离状态、`tavo.get/set` + localStorage 双写持久化 |
| Prompt | 按已启用功能拼接注入提示词 |
| Views | 太极八卦主界面 + 各功能纯渲染 |
| App | DOM 挂载、事件委托、Hooks 绑定 |

## 三、主界面：太极八卦布局

**先天八卦**排列（乾上坤下、离左坎右），中央为太极。

```
                 乾 · 本命玉牌
        兑 · 交流讯息        巽 · 天下论坛
   离 · 记事玉册      ☯太极      坎 · 芥子空间
        震 · 交易坊市        艮 · 玉兆管理
                 坤 · 天下舆图
```

### 卦位映射与取象理由

| 卦 | 卦象 | 功能 | 取象理由 |
| --- | --- | --- | --- |
| 乾 ☰ | 天 | 本命玉牌 | 乾为天、为君、为本我 → 角色根本身份 |
| 兑 ☱ | 泽 | 交流讯息 | 兑为口、为说 → 沟通传讯 |
| 离 ☲ | 火 | 记事玉册 | 离为文明、为文章 → 刻录记事 |
| 震 ☳ | 雷 | 交易坊市 | 震为动 → 坊市喧嚣、交易往来 |
| 巽 ☴ | 风 | 天下论坛 | 巽为风 → 言论如风传四方 |
| 坎 ☵ | 水 | 芥子空间 | 坎为隐伏、为藏 → 芥子纳须弥的隐秘空间 |
| 艮 ☶ | 山 | 玉兆管理 | 艮为止 → 掌开阖启停（禁用/启用） |
| 坤 ☷ | 地 | 天下舆图 | 坤为地 → 承载山川舆图 |

> 对卦两两相对、天然对称：乾-坤、离-坎、兑-艮、震-巽，布局平衡。

### 太极中枢

- 太极阴阳鱼**缓慢旋转**，是玉兆的"器灵/核心"。
- 作为**状态指示**：同步完成 / 部分 / 等待，用光晕颜色表达。
- 点击太极 → 回到主界面或打开「玉兆管理」。

### 卦位交互

- 每个卦位 ＝ 一个功能入口，样式为**卦象符号 + 卦名 + 五行强调色**（卦名随界面语言）。
- 被「玉兆管理」封印的功能 → 卦位呈**封印暗态**（灰化 + 角标，角标文案随语言）。
- 封印开关在「玉兆管理」页内切换（无长按/右键快捷封印）；长按悬浮入口 900ms 复位其位置。

### 各卦五行强调色

| 卦 | 色 | 卦 | 色 |
| --- | --- | --- | --- |
| 乾 | 金白 | 巽 | 青绿 |
| 兑 | 银泽 | 坎 | 玄蓝 |
| 离 | 赤红 | 艮 | 岩灰 |
| 震 | 苍碧 | 坤 | 土黄 |

## 四、八大功能详细设计

### 1. 交流讯息（兑）— 参考微信

- **联系人（一对一）**：道号/姓名、关系、最后传讯时间、未读数、消息列表。消息形态为"传音符"。
- **群聊（一对多）**：群名（如"青云宗·内门"）、成员数、消息含发送者。
- 数据行：`contact / msg / group / gmsg` 四类。

### 2. 天下论坛（巽）

- 所有持玉兆者可发帖；分**版块**（修炼心得 / 法器交流 / 悬赏 / 闲聊）。
- 帖子：作者、作者身份、标题、正文、"共鸣"（点赞）、评论列表。
- 数据行：`post / comment`。

### 3. 记事玉册（离）

- 备忘录：**分类（文件夹）+ 条目**；条目可加"禁制"（锁定）。
- 数据行：`folder / note`。

### 4. 交易坊市（震）

- **坊市行情**：在售物品（名、品阶、描述、价格、卖方）。
- **拍卖行**：物品、起拍价、当前价、剩余时间、出价人数。
- **订单**：物品、状态（待付款 / 已拍下 / 交易中 / 已完成 / 已取消）、价格、买/卖角色。
- 数据行：`listing / auction / order`。

### 5. 芥子空间（坎）

- **钱财**：种类（下品/中品/上品灵石、仙晶、贡献点…）+ 数额。
- **物品**：名、数量、品阶、描述。
- 数据行：`currency / item`。

### 6. 天下舆图（坤）

- **当前所在地**：地名、所属域、说明。
- **近期行踪**：时间、地点、动作/事件（轨迹）。
- 数据行：`current / track`。

### 7. 本命玉牌（乾）

- 分组字段：**基本**（名字/性别/身高/体重）、**仪容**（外貌/穿着）、**修为**（灵根/体质/境界/状态）、**隐秘**（隐秘信息）。
- 数据行：`field｜分组｜字段名｜值`。

### 8. 玉兆管理（艮）

- **本地系统页**，不由模型生成。
- 列出其余 7 个功能的**启用/封印**开关。
- 禁用后：该卦位封印、提示词不再请求该功能、不可打开。
- 这是**省 token 的核心机制**——只同步启用的功能。

## 五、数据协议设计

模型每轮输出一个 `<yz_jade>` 包裹块，内含 `<yz_meta>` + 各启用功能的子块。格式沿用"非 JSON、一行一条、`｜` 分字段"：

```
<yz_jade>
<yz_meta>
turn｜本轮唯一ID｜角色名｜本轮变化摘要｜full
</yz_meta>
<yz_msg>
contact｜id｜道号｜关系｜时间｜未读｜预览
msg｜联系人id｜消息id｜self或other｜时间｜内容
group｜群id｜群名｜成员数｜时间｜未读｜预览
gmsg｜群id｜消息id｜发送者｜self或other｜时间｜内容
</yz_msg>
<yz_forum>
post｜id｜作者｜身份｜版块｜时间｜标题｜正文｜共鸣数
comment｜帖子id｜评论者｜时间｜内容
</yz_forum>
<yz_notes>
folder｜id｜名称｜数量
note｜id｜文件夹id｜时间｜true或false｜标题｜正文
</yz_notes>
<yz_market>
listing｜id｜物品名｜品阶｜描述｜价格｜卖方
auction｜id｜物品名｜品阶｜描述｜起拍价｜当前价｜剩余时间｜出价人数
order｜id｜物品名｜状态｜价格｜时间｜买或卖
</yz_market>
<yz_space>
currency｜种类｜数额
item｜id｜名称｜数量｜品阶｜描述
</yz_space>
<yz_map>
current｜所在地｜所属域｜说明
track｜id｜时间｜地点｜动作
</yz_map>
<yz_tablet>
field｜分组｜字段名｜值
</yz_tablet>
</yz_jade>
```

**校验约束**（仿 `assessSnapshot`，逐功能判定"完整同步"）：

- 交流讯息：≥ 2 联系人且每人 ≥ 2 条消息
- 天下论坛：≥ 2 帖且每帖 ≥ 1 评论
- 记事玉册：≥ 2 分类且 ≥ 3 条备忘
- 交易坊市：行情 / 拍卖 / 订单均非空
- 芥子空间：钱财 + 至少 1 类物品
- 天下舆图：当前位置 + ≥ 2 条行踪
- 本命玉牌：四组字段齐全

未达标 → 保留旧数据、标记 `partial`。

### 增量同步扩展（v2.0.0 → v2.1.0 diff 协议）

`<yz_meta>` turn 行第 5 字段取值：`full`（缺省同义，所有已启封分区整块完整输出）、
`part`（v2.0.0 仅含变化分区，v2.1.0 起仅作历史消息水化兼容保留）与 `diff`（v2.1.0
起常规轮次）。

**diff 轮**（git diff 语义）：分区内的行以 `+`/`-` 前缀输出——`+行` 为 upsert（id
已存在则整行替换，不存在则受分区上限约束追加；联系人/群行替换时保留其消息），`-行`
为删除（只给定位字段：id 行删 id、field 删组+键、currency 删种类、comment 删整行内容）；
未提及的行客户端原样保留。识别规则：meta 声明 `diff`，或任一分区出现 +/- 操作行（声明
缺失时以行形态为准——防止模型只输出变化行却被按 full 整块替换清掉未提及数据，这正是
v2.0.3 之前满屏红色感叹号与数据碎片化的根因）。无前缀行在 diff 分区内宽松按 `+` 处理。
没有任何变化时只输出 `<yz_meta>`（meta-only：状态保持、摘要更新、不动 revision）。

提示词始终注入 `<yz_current>` 当前数据基线（`buildCurrent(state, flags)` 把 state 按
协议行语法序列化，内部 `yzc_` 容器标签与输出格式同构但不会与 `<yz_jade>` 解析冲突；
封印功能不进；行数上限与解析器一致）。基线是模型唯一的数据事实来源（协议块已从历史
剥离，正文不含旧数据），diff 轮对照基线只输出变化行——输出 token 从「每轮全量数据」
降为「每轮变化行」。强制全量轮（状态 empty/revision===0，或任一封印切换后的第一次生成，
内存 `flagsDirty` 标记）仍输出完整区块并在成功后清除标记；diff/part 轮不清除。v2.0.0
的 `sync_scope` 设置（full/smart 二选一）已删除——diff 是唯一常规路径。

`sync.issues` 非空时提示词回显 ≤3 条未达标项（经 catalog 按 lang 翻译），要求模型用
`+ 行`补齐。剥离通道（TAG_PREFIX/stripBlocks/stripStreamTail/DOM 扫描）覆盖 `digest`
（历史残留）与 `current` 标签，防模型复读基线进正文。`lines()` 的项目符号剥离要求后随
空白（`^[-•*]\s+`），无空格前缀保留给 diff 操作行。

客户端评估矩阵：

| 场景 | 判定 |
| --- | --- |
| full | 与 v1.6 一致；skip 行视同分区缺失 |
| part 出现且达标 | 应用该分区；全部达标 → complete |
| part 出现不达标 | 丢弃保旧、partial、issue 来自本轮出现分区 |
| part skip 且旧数据达标 | 静默通过：不计 issue、不进 applied |
| part skip 但旧数据缺失 | 沿用现有 issue code、partial |
| part meta-only 且 revision>0 | 状态保持、摘要更新、不弹 Toast |
| part meta-only 且 revision===0 | invalid（防开局白卷） |
| diff 触及分区合并后达标 | 落盘合并结果、进 applied、revision+1 |
| diff 触及分区合并后不达标 | 不落盘保旧数据、记 issue（供 + 行修复回声） |
| diff 未触及分区 | 数据不动；重推导达标性（旧数据不达标 → issue 回显自愈，达标 → issue 清除） |
| diff meta-only 且 revision>0 | 状态保持、摘要更新、不动 revision |
| diff 全部不达标且 revision>0 | partial（旧数据仍在，区别于 full 的 invalid） |

### 注入预算与世界书归档（v2.1.0 token 上限）

diff 格式只降「输出」token；每轮注入的 `<yz_current>` 基线仍随数据增长（消息/笔记/帖子
上限全满时可达数万字符），多轮后 token 消耗滚雪球。v2.1.0 给注入侧加双保险：

- **最近窗口 + 归档摘要**：`buildCurrent` 每实体只序列化最近 `RECENT_MSG_ROWS=6` 条消息
  （联系人 20 条上限、群 24 条上限不变，超限由 `tail()` 保尾截断——最旧淘汰、最新保留，
  这是多轮后「群聊看不到最新消息」的修复）；笔记/帖子/挂单/拍卖/物品窗口为 3/3/6/6/10，
  超窗条目以 `archived｜类型｜id｜摘要` 行出现（该行类型不在任何解析器白名单里，模型
  复读也不会污染解析）。提示词规则：归档条目不要复述、不要用 + 行整行替换（会丢正文），
  只能 - 行删除。所有行的时间字段一律使用绝对日期（如 丙午年五月十二 午时），禁止
  今日/昨日 等相对表述——消息进入世界书归档后可能在剧情日期更晚时被召回，相对时间
  会误导模型。
- **全局字符上限**：`MAX_BASELINE_CHARS=9000`——所有行计入预算，超限按三级淘汰：
  先丢其它功能的明细行（消息/笔记/帖子正文，消息在世界书有召回），再丢 tablet 字段行
  （角色设定排最后），最后丢归档摘要行；实体标识行（contact/group/folder/order/
  currency/track 等，diff 定位与存在性依赖）永不淘汰。数据再大每轮注入量被控制在
  上限附近（标识行极端满配时可达 ~1 万字符的量级，仍与轮次无关、不再滚雪球）。
- **世界书召回（相关联的数据）**：窗口外的历史消息镜像到插件管理的世界书
  （`tavo.lorebook`，书名为 `玉兆档案·<chatId>`），每个联系人/群一个关键词条目
  （keywords=实体名、scanDepth=4、atDepth 注入、system 角色、内容 ≤6000 字符、只读
  声明脚注）。正文提及实体名时 Tavo 才注入其完整归档——召回量=本轮相关实体，而非全部。
  归档与基线窗口共用 `RECENT_MSG_ROWS` 切分，两通道不重叠不遗漏。同步时机：msg 分区
  落盘后的后台刷新（generation:success / message 钩子 / 历史重建 / 管理页清空与导入），
  带 busy+pending 防重入；挂接聊天用 `tavo.chat.current()` 读既有世界书列表后合并
  （chat.update 整体替换，不覆盖用户自己的世界书），且只在目标即当前聊天时挂接。
  世界书能力缺失/API 失败一律静默降级（基线窗口照常工作，无报错面）。

### 同步诊断与卦位徽标（v2.0.0 可观测层）

- `renderSyncDetail(state)` 单一渲染器三入口共用：主界面 `.yz-sync` 行按钮、太极核心
  按钮（功能页内回主界面、主界面内开详情）、管理页顶部折叠诊断区。展示状态徽标/摘要/
  turnId+lastSource/applied 分区/issues 翻译/lastError（非空才显示）/更新时间（手工
  `YYYY-MM-DD HH:mm` UTC 格式，不用 toLocaleString）/容量进度条（>80% 警示色）/
  revision/processedTurns 数/chatId。
- 管理页数据操作：单功能清空（行尾次级按钮两击确认，3 秒超时还原，纯函数
  `nextWipeState` 直测；执行 = 分区重置 blank + 落盘，不动 processedTurns）、存档导出
  （只读 textarea + 全选复制）、导入（normalizeState 校验 + 超 MAX_SNAPSHOT_BYTES 拒收）。
- 卦位三态徽标互斥优先级警示 > 未读 > 新：警示取 `sync.issues` path 前缀映射功能；未读
  为 contacts+groups unread 总和（仅兑卦，>99 显示 `99+`）；新为 `sync.applied` 含该功能
  且不在 `sync.appliedSeen`（持久化于 state.sync，openFeature 并入、下轮 applied 移除），
  仅呼吸光效不加文字角标。封印态不显示任何徽标；aria-label 附徽标语义。
- 侧边栏动作：`open-jade`（复用 open()）、`resync-history`（rebuildFromHistory + toast）。

## 六、双玉兆架构：玩家域 + 传讯通道（一期核心）

**定位**：玩家 = 安装插件的 Tavo 用户；玉兆数据以游戏角色为主体（玩家"偷窥"角色视角）。
玩家交互需要独立「玩家域」，与「角色域」数据隔离但可互通。

### 域模型

- **角色域**（既有 `state`）：模型协议数据，经评估/指纹/预算/世界书归档，逻辑完全不变。
- **玩家域**（新增 `playerState`）：与角色域同构的私有数据容器（tablet/chats/notes/
  market 订单/space/map），**不含** forum 与 market 行情/拍卖（公开数据）——
  **不经模型评估（写什么是什么）**；无 revision/processedTurns/指纹/hydration/pendingFull，
  两域绝不共享这些字段。
- **存储三层**：宿主 chat 键（`yz_jade_player_v1`）+ 本地镜像（`yz-jade-player-v1:<chatId>`）
  + 全局备份（`yz-jade-player-v1-backup:<chatId>`），按 updatedAt 取新；
  **不进世界书**（快照/归档只服务角色域）。
- **公开数据跨域一致**：天下论坛、坊市行情/拍卖永远渲染角色域数据并带「公开」标识；
  私有数据随域切换数据源。域切换按钮在顶栏：只换数据源不换 UI。
- 玩家身份 = `{{user}}`（宿主用户身份名 `chat.persona.name`），缺失时回退 catalog 文案
  （zh「道友」/ en「Fellow Daoist」）。

### 传讯通道（唯一跨域写入点）

- 玩家域「交流讯息」= 固定「与角色传讯」会话（`yz-character` 线程）；底部输入框发讯。
- 发送：玩家消息 `pm-<seq>` 写入玩家线程（self），并投递为角色域 `yz-player` 联系人的
  other 消息（真实事件，不经模型评估）。**幂等**：双方按消息 id upsert，重复同步无副本；
  并发同步（send 内部 + 显式调用）经「await 先于检查/变更」的同步段保证不重复建联系人。
- 角色域评估豁免：`yz-player` 联系人不参与联系人数与每人消息数底线（玩家发讯不拉低
  达标度），也不凑联系人数（真实缺口仍暴露）。
- 未读数由客户端维护（seq > 已读游标的 other 消息数），模型不得改写（提示词规则）。
- **已读游标**（`state.sync.playerReadCursor`，注入即已读）：prepare 注入基线后推进，
  未读清零，消息本体保留在角色域线程；sync 整体重建时必须保留该字段（防跨轮反弹）。
- **回复**：模型在后续轮次以普通 `+msg｜yz-player｜新id｜self｜绝对时间｜正文` 自然回复；
  通道把角色域 self 消息镜像回玩家线程（other + reply 标记）。
- **UI 状态标记**：已回（线程中该消息之后存在角色回复）> 已读（seq ≤ 游标且已投递）>
  已送达；玩家域主界面显示 已送达/已回 统计。
- **注入预算**：未读行计入 9000 预算但淘汰优先级最高——四级淘汰
  （明细行 → tablet 字段 → 归档行 → 未读行，最后一级只在极端超限时让位）；
  每轮未读全行上限 `MAX_PLAYER_UNREAD_ROWS = 5`，超出给一条 `unread｜yz-player｜N 条未读`
  摘要行；已读消息按最近窗口 6 条 + archived 行常规处理。
- 封印 msg 后通道停止工作（不注入/不镜像），双方各自保留数据；历史重建/清空后
  `syncPlayerChannel` 按玩家域补投回角色域。
- 玉兆管理（艮）是角色域本地系统页：玩家域内封印卦位，点击仅提示不可用。

### 玩家域 CRUD（二期）：玩家直写，不经模型评估

- 记事玉册：玉册夹 / 备忘 的增删改（表单页：新建无预填、编辑预填；备忘含禁制锁定
  checkbox 与隐藏 folderId；删除玉册夹级联删除其下备忘）。
- 芥子空间：物品（名称/数量/品阶/描述）与钱财（种类+数额；种类为唯一键，重命名 =
  移除旧种类写入新种类）的增删改。
- 坊市订单：创建/编辑/删除，方向（买入/卖出）归一为 buy/sell，时间戳 = 本机 UTC。
- 交互：列表页底部「新建」CTA + 行尾「编辑」按钮（`editableListRow` 主区按钮 +
  编辑按钮，button 嵌 button 非法的处理与管理页同）；表单页保存/删除（删除走两击
  确认，复用 `nextWipeState` 武装状态机，key = kind:id，导航即复位）。
- 数据形态审计：全部直写 `playerState`，不经模型、不进世界书、不触发角色域任何逻辑；
  id 确定性生成（`playerNextId`：pf-/pn-/pi-/po-<n>，重载不冲突）；
  写入后按各分区归一器重排（文件夹计数派生等）；校验失败返回 reason 供 UI 提示。
- 角色域页面不受影响：CRUD 控件只出现在玩家域（renderNotes/renderSpace/renderMarket
  的 player 标志），角色域列表保持纯只读。

## 七、状态与持久化

- 按 `chatId` 隔离；`chat:opened` 切换、`message:deleted` 从历史重建。
- `processedTurns` 去重，防同一轮重复应用；重复投递的轮次不重复落盘。
- 快照超阈值（200KB）拒收；解析失败转 Toast 并保留旧数据。
- **多级备份链（v2.1.0 更新/卸载容灾）**：存储回退链 = 宿主 chat 键 → 本地镜像
  （`yz-jade-v1:<chatId>`）→ 全局备份键（`yz-jade-v1-backup:<chatId>`，save 时随 chat
  键双写）→ 世界书快照（`玉兆档案·<chatId>` 书内 `yz-snap` 禁用条目，内容为整份状态
  JSON，≤100KB 才写）→ 空白。任何非宿主来源读到的数据都回写宿主 chat 键与本地镜像，
  避免每次开聊重走恢复链。世界书是用户数据，卸载插件时最可能存续——历史正文被剥离后
  无法从消息重建，这是卸载重装后唯一的完整恢复路径。
- **版本迁移（v2.1.0 防旧数据粘滞）**：状态记录 `pluginVersion`；加载/导入时发现版本
  变化即置持久化强制全量标记 `state.pendingFull`（封印切换同样置位，替代内存
  flagsDirty 重启丢失的盲区）。下一轮生成按新提示词强制全量重写全部数据（旧格式行、
  相对时间等一次性刷新），成功应用一轮完整全量（`applySnapshot` 中非 diff/part 且
  assessment.ok）后清除标记；diff/part 轮不清除。全量轮提示词含「时间字段为相对表述
  的行本轮一律改写为绝对日期」规则——否则「基线数据视为既定事实」会诱导模型把旧相对
  时间原样照抄进全量输出，粘滞修复被规则自相矛盾抵消。
- **旧数据兼容**：全部历史版本 schemaVersion 恒为 1 且字段集是当前字段集的子集，
  normalizeState 白名单重建双向吸收（缺失补空、畸形消毒、超限截断），结构上不存在
  不兼容路径；若未来发生结构性变更，以 schemaVersion 显式迁移 + pendingFull 全量重建
  兜底，而不是原地改写旧档。
- 双写单向规则：host（`tavo.set(key, value, 'chat'/'global')`）为准，读成功即回写 localStorage 镜像；local 仅作宿主存储断链兜底。

## 八、视觉风格

- **玉质**：青玉/白玉透光、温润反光。
- **纹饰**：祥云纹、雷纹、篆书铭文环绕玉盘。
- **光效**：灵气流光、卦位呼吸光晕、太极缓转。
- **字体**：标题篆/隶，正文保证可读。
- 悬浮入口为一枚**玉佩/玉简**（可拖拽），点击展开太极八卦盘。

## 九、设置项（settings.schema）

| key | 类型 | 说明 |
| --- | --- | --- |
| `enabled` | switch | 启用玉兆 |
| `auto_strip` | switch | 自动隐藏正文中的玉兆数据块 |
| `lang` | select | **提示词语言**（中/EN）——决定注入提示词与生成内容的语言 |

界面语言不设开关：跟随宿主 App 语言（`tavo.plugin.i18n`），语言切换时全量重渲染。

## 十、关键实现决策

| 决策 | 说明 |
| --- | --- |
| 全屏 UI 直接挂载，不走 `htmlFragments` | 法器是全视口模态 + 全局悬浮入口，需在 Hook 期间独立于聊天页生命周期工作；宿主没有「当前是否聊天页」的查询 API，用 `chatActive`（chat:opened/chat:closed 维护）门控 FAB 显隐，配合禁用收起并隐藏 FAB、`visibilitychange` / window focus 刷新 |
| z-index 取单档 `Z_INDEX_TOP = 2147483646` | overlay 与 FAB 共用同一常量，全屏模态语义需要顶层；刻意避开 2147483647 最大值，给与宿主或其它插件的层级协调留一档余量 |
| FAB 默认位置 `bottom:96px; right:16px` | 由 `FAB_MARGIN_BOTTOM/FAB_MARGIN_RIGHT` 常量统一供 CSS 初始位置与长按复位/管理页复位按钮使用，保证「复位」即回到初始位置；真机（手机/平板/桌面）仍需逐一验证默认与复位位置不遮挡发送等控件 |
| generation:success 内部顺序不变式 | 读 payload → 同步剥离事件正文 → 异步应用快照 → 后台落盘。该 Hook 每个 handler 只有约 5 秒预算且超时整体丢弃：剥离是阻止协议块进入已保存消息的关键动作，纯字符串操作，必须在任何 await 之前完成 |
| 持久化为后台串行队列 | 内存态同步更新，写盘不阻塞调用方（同 key 写入按入队顺序生效）；重复投递轮次不再触发落盘；序列化体积超限仅在 yz_debug 下告警 |
| 正文剥离双通道 | 权威路径在 Hook 层：generation:success 同步剥离事件正文、message hook 应用快照；兜底路径：MutationObserver 收集增量变更记录（220ms 防抖、队列上限），只扫描新增/变化的文本节点，启动时做一次全量扫描；两条路径都受 `enabled()` + `auto_strip` 门控 |
| 水化版本标记 | `state.hydration.sig = 消息条数:末条id`，未变化时跳过历史全文扫描。已知盲区：编辑中间楼层不改变签名，由「message:updated 不带信封 → 去抖 `rebuildFromHistory`」兜底；消息删除同样走全量重建 |
| 内存聊天缓存上限 | 按 chatId LRU 只保留最近 5 个会话的内存态；淘汰无损——每次写入已落盘，重进时从宿主/本地重新加载 |
| 历史水化只查 assistant | role 过滤枚举按 API 文档只有 system/assistant/user；群聊多角色以 `role:'assistant'` + 不同 characterId 表达，不做别名兜底查询 |
| 宿主行为依赖清单（每次升级 Tavo 后回归） | ① entry 可访问顶层 window/document 并向 body 挂载（跨域 iframe 时回退当前文档）；② Pointer Events、MutationObserver、TreeWalker 可用；③ `tavo.get/set` 与 localStorage 可用。全屏 UI 与剥离通道依赖以上行为，宿主若提供页面级挂载点应迁移并删除手工门控 |
| issues 只存 `{path, code}` | 文案在 catalog（`assess.issue.*`），展示时按界面语言翻译 |

## 十一、开发与修复日志

| 版本 | 内容 |
| --- | --- |
| P1 | 协议（`<yz_jade>/<yz_meta>/<yz_tablet>`）+ Core + 太极八卦主界面 + 本命玉牌 |
| P2 | 交流讯息（`<yz_msg>`）+ 记事玉册（`<yz_notes>`）；v1.1.1 修复首轮反馈问题 |
| v1.2.0 (P3) | 天下论坛 + 交易坊市 + 芥子空间；修卦位键名错位 |
| v1.3.0 (P4) | 天下舆图 + 玉兆管理封印机制 + i18n 迁移 `tavo.plugin.i18n` |
| v1.3.1 | 真机修复：点击卦位整屏空白（page 容器 `hidden` 属性 vs class 切换）；FAB `[hidden]` 规则 |
| v1.3.2 | 太极图标精确居中、移除其下「玉兆」字样 |
| v1.3.3 | 封印后 sync 恒 partial（assess/applySnapshot 按 flags 判定）；seal 角标入 catalog；FAB aria-label 随语言刷新；翻译字典缓存 onChange 失效 |
| v1.4.0 | `chat:closed` 收起/禁用收起/visibilitychange 刷新；全封印跳过注入；manifest 补 `cover`；水化版本标记；switchChat load 竞态保护；en 成员单位；issues `{path, code}` 入 catalog；oversized Toast；FAB 默认位置上移 96px；焦点管理；持久化路径 sanitize |
| v1.5.0 | FAB 复位与默认位置共用 `FAB_MARGIN_*` 常量（修复复位回到旧 64px 的不一致）；语言切换重渲染 shell 静态文案（顶栏品牌 + aria-label）；非聊天页隐藏 FAB（chatActive 门控）；正文剥离改定向增量扫描并加 `enabled()` 门控；管理页新增显式复位按钮；模态 Tab 焦点陷阱；`avaFallback` 入 catalog；permissions 移除未使用的 `input`；移除失效的 wheel JS hack 改用 `overscroll-behavior:contain` |
| v1.6.0 | generation:success 重排为「先同步剥离、后应用快照」，持久化改后台串行队列并收紧重复轮次落盘；host 读成功回写本地镜像；移除 role:'character' 兜底查询；编辑旧楼层删除协议块触发去抖重建；聊天缓存 LRU 上限 5；统一 dbg 调试日志（yz_debug=1）；z-index 收敛单档 `Z_INDEX_TOP` 常量；提示词补 msg/gmsg 方向取值硬约束；window focus 补充刷新 FAB |
| v2.0.0 | P1+P2 合并发布。可观测层：同步诊断详情页（三入口共用 renderSyncDetail）、管理页单功能两击清空/存档导出导入、侧边栏动作（open-jade/resync-history）、卦位三态徽标（警示>未读>新，appliedSeen 持久化）。增量同步：meta 第 5 字段 part/skip 协议、buildDigest 确定性摘要注入、强制全量轮（flagsDirty）、issue 回声 ≤3 条、sync_scope 设置（默认 full）、剥离通道覆盖 digest 标签、part 轮失败最差只到 partial |
| v2.0.1 | 真机反馈修复：FAB 拖拽跟手（`touch-action:none` 防触摸滚动劫持 + `.dragging` 态禁用位置过渡，消除 200ms 缓动滞后）；卦位「新同步」去掉文字角标只留呼吸光效（文字遮挡卦名） |
| v2.0.2 | FAB 图标由 `◈` 字符重绘为玉璧 SVG（渐变玉质 + 刻纹环 + 璧孔 + 高光，`FAB_ICON` 常量供 FAB 与管理页复位行共用）；修复点击方形高亮——FAB 在 overlay 之外不继承 `-webkit-tap-highlight-color`，现于自身声明禁用，按压反馈改为圆形缩放 + 提亮 |
| v2.0.3 | 修复多轮连续性：提示词注入 `<yz_current>` 当前数据基线（`buildCurrent`，`yzc_` 容器防解析冲突，行数上限对齐解析器），模型必须沿用既有 id 与未变化行；smart 增量对照由 digest 改为完整基线，删除 `buildDigest`/`DIGEST_CAPS` 机制；剥离通道新增 `current` 标签 |
| v2.0.4 | 修复重新生成/继续丢关联的三个根因：① prepare 竞态——handler 改异步，注入基线前 `resolveCurrentChatId` + `settle()` 等待 switchChat 加载/水化收尾（插件重载后否则读到空白态注入空基线）；② 空白占位顶掉持久化状态——switchChat 的内存保留条件从「存在」改为「真正写入过」（revision/sync.updatedAt/processedTurns 任一非零），仅被 `current()` 读过的空白占位不再丢弃 loaded、也不再触发空白态回写覆盖存储；③ 同 turnId 去重误杀 regen 轮——`processedTurns` 改存 `turnId@内容哈希` 指纹，内容有变即应用、同内容双通道投递仍去重；提示词补「重新生成/续写同样受基线约束」 |
| v2.1.0 | diff 增量协议（git diff 语义）：`+行` upsert / `-行` 删除（定位字段），客户端与基线合并，模型每轮只输出变化行（省 token）；未提及分区原样保留——根治 v2.0.3 提示词「只输出变化行」与 full 整块替换语义冲突导致的满屏红色感叹号与数据碎片化。识别：meta `diff` 或行形态（+/- 前缀）；`lines()` 项目符号剥离改要求后随空白。diff 轮评估在合并结果上进行（触及不达标不落盘保旧数据 + issue；未触及分区重推导达标性可自愈）。issue 回声改「用 + 行补齐」；`applyText.full` 排除 diff 轮（flagsDirty 只能被真全量轮清除）；删除 `sync_scope` 设置（diff 为唯一常规路径，part/skip 解析保留作历史水化兼容）；注入预算与世界书归档（token 雪球根治 + 两处数据修复）。① 修复多轮后群聊看不到最新消息：消息满员（联系人 20/群 24）时新消息被静默丢弃——改保尾截断（`tail()`），满员追加收下最新、淘汰最旧；② 修复记事玉册数量统计：文件夹 count 改按实际笔记派生（不再信任模型声明值，diff 增删笔记后计数自动正确）；③ 基线窗口化：`buildCurrent` 每实体只注入最近 6 条消息（笔记/帖子/挂单/物品同理），超窗条目以 `archived｜类型｜id｜摘要` 行概括（不在解析器白名单，复读不污染）；④ 发送上限 `MAX_BASELINE_CHARS=9000`：三级淘汰（明细 → tablet 字段 → 归档行），标识行永不淘汰，注入量封顶不再随数据滚雪球；⑤ 世界书召回：窗口外历史消息镜像到 `tavo.lorebook`（`玉兆档案·<chatId>`，每联系人/群一个关键词条目，keywords=实体名/scanDepth=4/atDepth 注入/只读脚注），正文提及实体时按关键词召回完整归档——只发最新+相关联数据；msg 落盘后后台同步（busy+pending 防重入），挂接聊天读既有列表合并不覆盖用户世界书，能力缺失静默降级；⑥ 提示词补归档规则（归档条目只可 - 行删除，不得 + 行整行替换）；⑦ 所有时间字段强制绝对日期（如 丙午年五月十二 午时），禁 今日/昨日 等相对表述（归档在剧情日期更晚时被召回）；⑧ 更新容灾：状态记 `pluginVersion`，版本变化/封印切换置持久化 `pendingFull` → 下一轮强制全量重写旧数据（防粘滞）；存储回退链 chat 键 → 本地镜像 → 全局备份键 → 世界书 `yz-snap` 快照条目（防卸载重装丢数据）；⑨ 修复重新生成/编辑消息后数据被清空：正文剥离通道移除了历史中的协议块（数据源），`rebuildFromHistory` 用空白覆盖内存态会把刚同步的数据清零——历史无协议块时保留现有数据（空聊天保持空白）；⑩ 修复跨聊天写入竞态：宿主 chat 作用域键绑定宿主「当前聊天」，插件与宿主短暂不同步（事件延迟窗口）时 `save` 会把 A 聊天数据写进 B 聊天的宿主键——写前复查宿主当前聊天（读与写同微任务无窗口），宿主已切走时跳过宿主键（本地镜像与全局备份照常落盘）；load 侧宿主键可能因写路径竞态留旧（含版本迁移空白占位），本地镜像带聊天标识、revision 更新时以镜像为准；`applyText` 的 full 判定精确为「评估达标的全量轮」（非 part/diff 且所有已启封分区达标），部分达标的全量轮不再清除强制重写标记 |
| v2.2.0（未发布） | 交互基座第一层：全部列表页与讯息详情页新增顶部检索框（`searchKw`/`contains`/`filterMatch`/`searchBox` 纯函数，8 页面接入），纯内存过滤不改数据；无命中专属空态；关键词随导航自动清空；输入框重渲染焦点/光标恢复支持连续键入；双语 catalog 新增 `runtime.search.*`。双玉兆一期：玩家域（`playerState`，与角色域同构但不含 forum/市场行情拍卖，无模型域字段；三层存储宿主 chat 键 `yz_jade_player_v1` + 本地镜像 + 全局备份，不进世界书；按 updatedAt 取新）；传讯通道（唯一跨域写入点）：玩家域固定「与角色传讯」会话 + 底部输入框，消息 `pm-<seq>` 投递角色域 `yz-player` 联系人（幂等 upsert，并发安全），未读数客户端维护，`assessMsg` 豁免玩家联系人（不拉低达标、不凑人数），prepare 注入基线即推进已读游标（`sync.playerReadCursor`，applySnapshot 重建 sync 时保留防反弹），模型以普通 `+msg` 自然回复并由通道镜像回玩家线程（已回标记）；未读行计入 9000 预算且淘汰优先级最高（四级淘汰：明细 → tablet → 归档 → 未读），每轮未读上限 `MAX_PLAYER_UNREAD_ROWS=5` 超出给 `unread｜` 摘要行；玩家身份 = `{{user}}`（`chat.persona.name`，缺失回退 catalog）；域切换按钮只换数据源不换 UI，公开数据（论坛/坊市行情拍卖）跨域一致带「公开」标识，管理页仅角色域可用（玩家域封印）；封印 msg 后通道停止、双方保留数据；历史重建/清空后按玩家域补投角色域。玩家域 CRUD（二期）：记事玉册（玉册夹/备忘增删改，删除级联，禁制锁定）、芥子空间（物品/钱财增删改，货币种类为键重命名）、坊市订单（增删改，buy/sell 归一）；`playerNextId` 确定性 id（pf-/pn-/pi-/po-<n>）；表单页保存/两击删除（复用 `nextWipeState`，导航复位）；列表页新建 CTA + 行尾编辑；全部直写玩家域不经模型、不进世界书，角色域列表保持只读。  角色域内容扩展·玉牌扩组：玉牌新增功法/羁绊两组（六组：基本/仪容/修为/功法/羁绊/隐秘）；组名别名归一（功法/心法/法诀/绝学/术法、羁绊/缘分/牵挂 + 英文），canonical 键（功法名/羁绊对象）diff 去重合并；达标底线功法/羁绊各 ≥1 条（与隐秘同为内容组），缺组 issue `tablet.gong`/`tablet.bond` 双语回显；引导行与约束文本随 PROMPT_FEATURES 同步扩展（六组齐全）；旧档无需版本迁移——diff 轮经 issue 回声自动补齐；删除组内最后一行被达标门禁拦截；buildCurrent/预算/指纹/备份链零改造（groups 为通用结构）。 |

冒烟测试基线：`node tests/smoke.mjs`（无外部依赖），覆盖 i18n 字典、manifest/catalog 结构（sidebar、
yz_current 基线回环与防累积）、Core 消毒与增量矩阵场景、Protocol 解析（mode/skip/digest/current/diff 边界）与剥离、Prompt 注入/
封印/diff 规则/回声/归档规则、diff 协议（upsert/删除/幂等/未声明 mode 识别/meta-only/底线保护/full 标志）、最新消息保尾保留（full/diff 满员追加）、文件夹计数派生、基线窗口与字符硬上限、世界书归档（建书/条目/关键词/挂接/窗口滑动/降级/封印）、Runtime 状态机（内容指纹去重/水化签名/切聊竞态/重载后 settle/空白占位保护/重建/appliedSeen/importState）、持久化队列与
缓存淘汰、Views 渲染（详情页/徽标/管理页/检索过滤/玩家域页面/域切换/公开标识/传讯状态）、FAB 拖拽/图标/点击反馈与 z-index 回归保护、版本迁移（pendingFull 置位/清除/跨轮保持）、备份恢复链（全局备份兜底/世界书快照恢复/回写）、无协议历史重建保护、跨聊天写入竞态（宿主键复查/镜像优先）、达标全量判定、双玉兆（玩家域归一/三层存储/传讯通道幂等/注入即已读/回复镜像/未读行优先级与上限/评估豁免/{{user}} 解析/CRUD 直写与校验/删除级联/表单视图），当前 **542 项全绿**。发布前仍需在
Tavo MCP 环境执行 `tavo_plugin_validate_manifest → tavo_plugin_audit → tavo_plugin_package`，并对
世界书归档（建书/关键词召回/挂接不覆盖既有）、FAB 默认/复位位置与拖拽跟手、卦位徽标排版、同步详情页键盘可达、触屏两击确认做手机、平板、桌面真机验证。
