# 玉兆 · 修仙法器设计文档

## 一、概念与命名

- **玉**：温润通灵之物，修真者随身法器的常见载体。
- **兆**：占卜之兆、先机之兆，与《周易》八卦同源。
- **玉兆** ＝ "以玉通灵、见微知著"。名字本身就绑定了八卦/占卜意象，因此主界面用**太极八卦**排布在主题上是自洽的，不是硬套。
- 定位：修仙世界的"手机"，但所有功能、文案、视觉、数据协议全部用修真文明包装。

## 二、整体架构（沿用本仓库插件模式）

协议驱动：

```
generation:prepare  注入提示词（只同步"已启用"的功能）
        ↓
模型正文正常结束 → 末尾输出 <yz_jade> 协议块
        ↓
generation:success  解析 → 校验 → 剥离正文协议块 → 渲染
```

模块分层：

| 模块 | 职责 |
| --- | --- |
| Core | 数据消毒、逐功能校验、状态机、去重 |
| Protocol | 解析 `<yz_*>` 标签 |
| I18n | 中/英双语包 |
| Runtime | 按 chatId 隔离状态、`tavo.get/set` + localStorage 双写持久化 |
| Prompt | 按已启用功能拼接注入提示词 |
| Views | 太极八卦主界面 + 各功能纯渲染 |
| App | DOM 挂载、事件委托、Hooks 绑定 |

## 三、主界面：太极八卦布局

**先天八卦**排列（乾上坤下、离左坎右），中央为太极。

```
                 乾 · 本命玉牌
        兑 · 交流讯息        巽 · 天下论坛
   离 · 记事玉册      ☯太极      坎 · 芥子空间
        震 · 交易坊市        艮 · 玉兆管理
                 坤 · 天下舆图
```

### 卦位映射与取象理由

| 卦 | 卦象 | 功能 | 取象理由 |
| --- | --- | --- | --- |
| 乾 ☰ | 天 | 本命玉牌 | 乾为天、为君、为本我 → 角色根本身份 |
| 兑 ☱ | 泽 | 交流讯息 | 兑为口、为说 → 沟通传讯 |
| 离 ☲ | 火 | 记事玉册 | 离为文明、为文章 → 刻录记事 |
| 震 ☳ | 雷 | 交易坊市 | 震为动 → 坊市喧嚣、交易往来 |
| 巽 ☴ | 风 | 天下论坛 | 巽为风 → 言论如风传四方 |
| 坎 ☵ | 水 | 芥子空间 | 坎为隐伏、为藏 → 芥子纳须弥的隐秘空间 |
| 艮 ☶ | 山 | 玉兆管理 | 艮为止 → 掌开阖启停（禁用/启用） |
| 坤 ☷ | 地 | 天下舆图 | 坤为地 → 承载山川舆图 |

> 对卦两两相对、天然对称：乾-坤、离-坎、兑-艮、震-巽，布局平衡。

### 太极中枢

- 太极阴阳鱼**缓慢旋转**，是玉兆的"器灵/核心"。
- 作为**状态指示**：同步完成 / 部分 / 等待，用光晕颜色表达。
- 点击太极 → 主界面内打开同步详情；功能页内点太极回到主界面。

### 卦位交互

- 每个卦位 ＝ 一个功能入口，样式为**卦象符号 + 卦名 + 五行强调色**（卦名随界面语言）。
- 被「玉兆管理」封印的功能 → 卦位呈**封印暗态**（灰化 + 角标，角标文案随语言）。
- 封印开关在「玉兆管理」页内切换（无长按/右键快捷封印）；长按悬浮入口 900ms 复位其位置。

### 各卦五行强调色

| 卦 | 色 | 卦 | 色 |
| --- | --- | --- | --- |
| 乾 | 金白 | 巽 | 青绿 |
| 兑 | 银泽 | 坎 | 玄蓝 |
| 离 | 赤红 | 艮 | 岩灰 |
| 震 | 苍碧 | 坤 | 土黄 |

## 四、八大功能详细设计

### 1. 交流讯息（兑）— 参考微信

- **联系人（一对一）**：道号/姓名、关系、最后传讯时间、未读数、消息列表。消息形态为"传音符"。
- **群聊（一对多）**：群名（如"青云宗·内门"）、成员数、消息含发送者。
- 数据行：`contact / msg / group / gmsg` 四类。

### 2. 天下论坛（巽）

- 所有持玉兆者可发帖；分**版块**（修炼心得 / 法器交流 / 悬赏 / 闲聊）。
- 帖子：作者、作者身份、标题、正文、"共鸣"（点赞）、评论列表。
- 数据行：`post / comment`。

### 3. 记事玉册（离）

- 备忘录：**分类（文件夹）+ 条目**；条目可加"禁制"（锁定）。
- 数据行：`folder / note`。

### 4. 交易坊市（震）

- **坊市行情**：在售物品（名、品阶、描述、价格、卖方）。
- **拍卖行**：物品、起拍价、当前价、剩余时间、出价人数。
- **订单**：物品、状态（待付款 / 已拍下 / 交易中 / 已完成 / 已取消）、价格、买/卖角色。
- 数据行：`listing / auction / order`。

### 5. 芥子空间（坎）

- **钱财**：种类（下品/中品/上品灵石、仙晶、贡献点…）+ 数额。
- **物品**：名、数量、品阶、描述。
- 数据行：`currency / item`。

### 6. 天下舆图（坤）

- **当前所在地**：地名、所属域、说明。
- **近期行踪**：时间、地点、动作/事件（轨迹）。
- 数据行：`current / track`。

### 7. 本命玉牌（乾）

- 分组字段：**基本**（名字/性别/身高/体重）、**仪容**（外貌/穿着）、**修为**（灵根/体质/境界/状态）、**隐秘**（隐秘信息）。
- 数据行：`field｜分组｜字段名｜值`。

### 8. 玉兆管理（艮）

- **本地系统页**，不由模型生成。
- 列出其余 7 个功能的**启用/封印**开关。
- 禁用后：该卦位封印、提示词不再请求该功能、不可打开。
- 这是**省 token 的核心机制**——只同步启用的功能。

## 五、数据协议设计

模型每轮输出一个 `<yz_jade>` 包裹块，内含 `<yz_meta>` + 各启用功能的子块。格式沿用"非 JSON、一行一条、`｜` 分字段"：

```
<yz_jade>
<yz_meta>
turn｜本轮唯一ID｜角色名｜本轮变化摘要｜full
</yz_meta>
<yz_msg>
contact｜id｜道号｜关系｜时间｜未读｜预览
msg｜联系人id｜消息id｜self或other｜时间｜内容
group｜群id｜群名｜成员数｜时间｜未读｜预览
gmsg｜群id｜消息id｜发送者｜self或other｜时间｜内容
</yz_msg>
<yz_forum>
post｜id｜作者｜身份｜版块｜时间｜标题｜正文｜共鸣数[｜player]
comment｜id｜作者｜时间｜内容
（post 行尾可选 owner：player = 玩家真实发帖，缺省 = 角色发帖）
</yz_forum>
<yz_notes>
folder｜id｜名称｜数量
note｜id｜文件夹id｜时间｜true或false｜标题｜正文
</yz_notes>
<yz_market>
listing｜id｜物品名｜品阶｜描述｜价格｜卖方
auction｜id｜物品名｜品阶｜描述｜起拍价｜当前价｜剩余时间｜出价人数
request｜id｜物品名｜品阶｜描述｜出价｜求购人
order｜id｜物品名｜状态｜价格｜时间｜买或卖
</yz_market>
<yz_space>
currency｜种类｜数额
item｜id｜名称｜数量｜品阶｜描述
</yz_space>
<yz_map>
current｜所在地｜所属域｜说明
track｜id｜时间｜地点｜动作
place｜id｜地点名｜所属域｜说明
</yz_map>
<yz_tablet>
field｜分组｜字段名｜值
</yz_tablet>
</yz_jade>
```

**校验约束**（仿 `assess`，逐功能判定"完整同步"）：

- 交流讯息：≥ 2 联系人且每人 ≥ 2 条消息，≥ 1 群且每群 ≥ 2 条消息（玩家传讯联系人豁免）
- 天下论坛：≥ 2 帖且每帖 ≥ 1 评论（玩家帖子豁免）
- 记事玉册：≥ 2 分类且 ≥ 3 条备忘
- 交易坊市：行情 / 求购 / 拍卖 / 订单四类均非空
- 芥子空间：钱财 + 至少 1 类物品
- 天下舆图：当前位置 + ≥ 2 条行踪 + ≥ 2 处地点名录
- 本命玉牌：六组字段齐全（基本/仪容/修为/功法/羁绊/隐秘，功法羁绊各 ≥1 条）

未达标 → 保留旧数据、标记 `partial`。

### 增量同步（diff 协议）

`<yz_meta>` turn 行第 5 字段取值：`full`（全量完整输出）、`diff`（常规增量轮次）与 `part`（历史兼容轮次）。

**diff 轮**（git diff 语义）：分区内的行以 `+`/`-` 前缀输出——`+行` 为 upsert（id
已存在则整行替换，不存在则受分区上限约束追加；联系人/群行替换时保留其消息），`-行`
为删除（只给定位字段：id 行删 id、field 删组+键、currency 删种类、comment 删整行内容）；
未提及的行客户端原样保留。识别规则：meta 声明 `diff`，或任一分区出现 +/- 操作行（声明
缺失时以行形态为准）。无前缀行在 diff 分区内宽松按 `+` 处理。
没有任何变化时只输出 `<yz_meta>`（meta-only：状态保持、摘要更新、不动 revision）。

提示词始终注入 `<yz_current>` 当前数据基线（`buildCurrent(state, flags)` 把 state 按
协议行语法序列化，内部 `yzc_` 容器标签与输出格式同构但不会与 `<yz_jade>` 解析冲突；
封印功能不进；行数上限与解析器一致）。基线是模型唯一的数据事实来源，diff 轮对照基线只输出变化行。
强制全量轮（状态 empty/revision===0，或任一封印切换后的第一次生成，内存 `flagsDirty` 标记）
仍输出完整区块并在成功后清除标记；diff/part 轮不清除。

`sync.issues` 非空时提示词回显 ≤3 条未达标项（经 catalog 按 lang 翻译），要求模型用
`+ 行`补齐。剥离通道（TAG_PREFIX/stripBlocks/stripStreamTail/DOM 扫描）覆盖 `digest`
与 `current` 标签，防模型复读基线进正文。`lines()` 的项目符号剥离要求后随空白（`^[-•*]\s+`），
无空格前缀保留给 diff 操作行。

客户端评估矩阵：

| 场景 | 判定 |
| --- | --- |
| full | 全量判定；skip 行视同分区缺失 |
| part 出现且达标 | 应用该分区；全部达标 → complete |
| part 出现不达标 | 丢弃保旧、partial、issue 来自本轮出现分区 |
| part skip 且旧数据达标 | 静默通过：不计 issue、不进 applied |
| part skip 但旧数据缺失 | 沿用现有 issue code、partial |
| part meta-only 且 revision>0 | 状态保持、摘要更新、不弹 Toast |
| part meta-only 且 revision===0 | invalid（防开局白卷） |
| diff 触及分区合并后达标 | 落盘合并结果、进 applied、revision+1 |
| diff 触及分区合并后不达标 | 不落盘保旧数据、记 issue（供 + 行修复回声） |
| diff 未触及分区 | 数据不动；重推导达标性（旧数据不达标 → issue 回显自愈，达标 → issue 清除） |
| diff meta-only 且 revision>0 | 状态保持、摘要更新、不动 revision |
| diff 全部不达标且 revision>0 | partial（旧数据仍在，区别于 full 的 invalid） |

### 注入预算与世界书归档

为控制每轮注入提示词的 `<yz_current>` 基线体积，采用双层预算机制：

- **条目级注入采样**：联系人/群聊/论坛帖子超过上限后（3/2/5），每轮只向
  基线注入采样子集。`sampleEntries`（CORE 纯函数，rng 可注入）：**强制包含集**（必定注入）=
  用户联系人（`c-` 前缀）、未读联系人、含 `pmg-*` 发言的群聊、`owner=player` 帖子与含
  `pmc-*` 评论或 `unread>0` 的帖子；其余按**活跃度加权随机**补足到上限——`score = 1 + 消息数/评论数`（钳制 20），
  `p_i ∝ score^γ`（γ=2）且钳制 `p_i ≥ β/n`（β=0.5）。
  未被采样的条目在基线中隐藏，diff 门禁防止对未注入条目的误修改。每轮独立采样不持久化。
  提示词约定：基线上未出现的条目仍存在，不得重建或删除。
- **最近窗口 + 归档摘要**：`buildCurrent` 每实体只序列化最近 `RECENT_MSG_ROWS=6` 条消息，
  超限由 `tail()` 保尾截断（最旧淘汰、最新保留）；笔记/挂单/拍卖/求购/地点/物品窗口为
  3/6/6/6/6/10，超窗条目以 `archived｜类型｜id｜摘要` 行呈现。
  时间字段统一使用绝对日期（如 丙午年五月十二 午时），禁止 今日/昨日 等相对表述。
- **帖子未读机制**：`post` 行第 10 字段 `unread` 表示新回复数，模型在回复后自增并在已读时以 `+post` 清零。
  unread>0 的帖子在采样时强制包含并在列表中高亮展示。
- **全局字符上限**：`MAX_BASELINE_CHARS=9000`。基线序列化后若超预算，按优先级自低向高逐级淘汰，
  实体核心标识行永不整行淘汰。
- **世界书召回**：窗口外历史消息镜像保存至世界书（`玉兆档案·<chatId>`），每个联系人或群聊对应一个关键词条目，
  正文提及对应实体时按需召回相关归档。地点名录同样通过专属关键词条目支持召回。

### 同步诊断与卦位徽标

- `renderSyncDetail(state)` 单一渲染器供三处入口共用：主界面状态按钮、太极中枢与管理页诊断区。
  展示状态徽标、摘要、轮次 ID、已应用分区、未达标 issue 翻译、最后错误、更新时间与容量进度。
- 管理页数据操作：单功能清空（两击确认、5 秒超时还原，清空所有空间中对应分区并写盘）、
  存档导出（文本复制，超限拦截）与导入（格式校验与超限拦截）。
- 卦位三态徽标互斥优先级为 警示 > 未读 > 新更新。封印功能不显示徽标。
- 侧边栏动作：打开玉兆与从世界书快照重建。

## 六、用户空间架构

每份聊天的数据由多个独立同构的用户空间承载，统一使用一套 UI。

### 6.1 数据模型

- 顶层 `state`：`{schemaVersion:2, chatId, pluginVersion, storageRevision, storageWriter,
  pendingFull, activeSpaceId, hydration, spaces:[Space], updatedAt}`。
  单存储键、单快照链；`storageRevision/storageWriter` 用于跨 tab 冲突拒写。
- `Space`：全套同构分区（tablet/chats/notes/forum/market/space/map）及空间元信息：
  `{id, name, isDefault, sendToAI, allowAIWrite, createdAt, revision, processedTurns,
  sync, updatedAt}`。
- **默认空间**（id `sp0`）：锚定 `{{char}}`，名称解析为角色名（`sync.roleName` 优先），
  强制可写，空间缺失时数据到达自动重建。
- 自定义空间：上限 6 个，名称唯一，支持独立重命名与 AI 开关。
- 状态载入时自动规范化旧版数据结构。

### 6.2 AI 协议：空间参数路由

- `turn` 行第 6 字段（可选）指定目标空间的 URI token；缺省时定向至默认空间。
- 拒写类型及 issue 记录：`space.unknown`（未知空间）、`space.denied`（`allowAIWrite=false`）、
  `space.full`（非默认空间拒绝全量覆盖轮次）。
- 基线注入按 `sendToAI=true` 的空间分组输出（自定义空间标注 `space` 属性）；条目采样配额在各空间均摊。
  完整性评估仅对默认空间生效。

### 6.3 真实发言门禁

- 用户内容标识：联系人 `c-<n>`、私讯 `pm-<n>`、群讯 `pmg-<n>`、评论 `pmc-<n>`、帖子 `owner=player`。
  模型不可覆盖、删除或伪造此类条目，仅允许在用户线程中追加新行。
- 用户线程未读数由客户端累计回复游标计算，不随窗口淘汰重置。

## 七、状态与持久化

- 按 `chatId` 隔离；切换聊天或消息删除时从世界书快照恢复。
- `processedTurns` 轮次去重，防重复投递。
- 快照超阈值（200KB）拒收。
- **世界书为主存储**：权威数据保存在世界书 `玉兆档案·<chatId>` 的分片快照条目
  `yz-snap-N`（每片 ≤90KB、最多 5 片）。本地镜像 `yz-jade-v1:<chatId>` 作为启动缓存。
  存储回退顺序：世界书 → 本地镜像 → 空白状态。清空操作写入空快照墓碑。
- **并发写安全**：跨标签页写入时校验 `storageRevision`，冲突时拒绝写入。BroadcastChannel 用于通知刷新，
  `storage` 事件作为兜底回退。
- **版本更新全量重写**：检测到版本变化或封印调整时，置位 `state.pendingFull`，促使下一轮生成输出完整全量数据以刷新字段。
- **格式约束**：导入与世界书快照仅接受当前 `schemaVersion:2` 空间结构。

## 八、设置项（settings.schema）

| key | 类型 | 说明 |
| --- | --- | --- |
| `enabled` | switch | 启用玉兆 |
| `auto_strip` | switch | 自动隐藏正文中的玉兆数据块 |
| `lang` | select | **提示词语言**（中/EN）——决定注入提示词与生成内容的语言 |

界面语言不设开关：跟随宿主 App 语言（`tavo.plugin.i18n`），语言切换时全量重渲染。

## 九、关键实现决策

| 决策 | 说明 |
| --- | --- |
| generation:success 内部顺序不变式 | 读 payload → 同步剥离事件正文 → 异步应用快照 → 后台落盘。该 Hook 每个 handler 只有约 5 秒预算且超时整体丢弃：剥离是阻止协议块进入已保存消息的关键动作，纯字符串操作，必须在任何 await 之前完成 |
| 持久化为后台串行队列 | 内存态同步更新，写盘不阻塞调用方（同 key 写入按入队顺序生效）；重复投递轮次不再触发落盘；序列化体积超限仅在 yz_debug 下告警 |
| 正文剥离双通道 | 权威路径在 Hook 层：generation:success 同步剥离事件正文、message hook 应用快照；兜底路径：MutationObserver 收集增量变更记录（220ms 防抖、队列上限），只扫描新增/变化的文本节点，启动时做一次全量扫描；两条路径都受 `enabled()` + `auto_strip` 门控 |
| 水化版本标记 | `state.hydration.sig = 消息条数:末条id`，未变化时跳过历史全文扫描。已知盲区：编辑中间楼层不改变签名，由「message:updated 不带信封 → 去抖 `rebuildFromHistory`」兜底；消息删除同样走快照恢复 |
| 内存聊天缓存上限 | 按 chatId LRU 只保留最近 5 个会话的内存态；淘汰无损——每次写入已落盘，重进时从宿主/本地重新加载 |
| 历史水化只查 assistant | role 过滤枚举按 API 文档只有 system/assistant/user；群聊多角色以 `role:'assistant'` + 不同 characterId 表达，不做别名兜底查询 |
| issues 只存 `{path, code}` | 文案在 catalog（`assess.issue.*`），展示时按界面语言翻译 |

## 十、变更记录

版本变更记录统一保存在 [CHANGELOG.md](CHANGELOG.md)（最新版本在最前）。
