# 玉兆 · 修仙法器设计文档

## 一、概念与命名

- **玉**：温润通灵之物，修真者随身法器的常见载体。
- **兆**：占卜之兆、先机之兆，与《周易》八卦同源。
- **玉兆** ＝ "以玉通灵、见微知著"。名字本身就绑定了八卦/占卜意象，因此主界面用**太极八卦**排布在主题上是自洽的，不是硬套。
- 定位：修仙世界的"手机"，但所有功能、文案、视觉、数据协议全部用修真文明包装。

## 二、整体架构（沿用本仓库插件模式）

协议驱动：

```
generation:prepare  注入提示词（只同步"已启用"的功能）
        ↓
模型正文正常结束 → 末尾输出 <yz_jade> 协议块
        ↓
generation:success  解析 → 校验 → 剥离正文协议块 → 渲染
```

模块分层：

| 模块 | 职责 |
| --- | --- |
| Core | 数据消毒、逐功能校验、状态机、去重 |
| Protocol | 解析 `<yz_*>` 标签 |
| I18n | 中/英双语包 |
| Runtime | 按 chatId 隔离状态、`tavo.get/set` + localStorage 双写持久化 |
| Prompt | 按已启用功能拼接注入提示词 |
| Views | 太极八卦主界面 + 各功能纯渲染 |
| App | DOM 挂载、事件委托、Hooks 绑定 |

## 三、主界面：太极八卦布局

**先天八卦**排列（乾上坤下、离左坎右），中央为太极。

```
                 乾 · 本命玉牌
        兑 · 交流讯息        巽 · 天下论坛
   离 · 记事玉册      ☯太极      坎 · 芥子空间
        震 · 交易坊市        艮 · 玉兆管理
                 坤 · 天下舆图
```

### 卦位映射与取象理由

| 卦 | 卦象 | 功能 | 取象理由 |
| --- | --- | --- | --- |
| 乾 ☰ | 天 | 本命玉牌 | 乾为天、为君、为本我 → 角色根本身份 |
| 兑 ☱ | 泽 | 交流讯息 | 兑为口、为说 → 沟通传讯 |
| 离 ☲ | 火 | 记事玉册 | 离为文明、为文章 → 刻录记事 |
| 震 ☳ | 雷 | 交易坊市 | 震为动 → 坊市喧嚣、交易往来 |
| 巽 ☴ | 风 | 天下论坛 | 巽为风 → 言论如风传四方 |
| 坎 ☵ | 水 | 芥子空间 | 坎为隐伏、为藏 → 芥子纳须弥的隐秘空间 |
| 艮 ☶ | 山 | 玉兆管理 | 艮为止 → 掌开阖启停（禁用/启用） |
| 坤 ☷ | 地 | 天下舆图 | 坤为地 → 承载山川舆图 |

> 对卦两两相对、天然对称：乾-坤、离-坎、兑-艮、震-巽，布局平衡。

### 太极中枢

- 太极阴阳鱼**缓慢旋转**，是玉兆的"器灵/核心"。
- 作为**状态指示**：同步完成 / 部分 / 等待，用光晕颜色表达。
- 点击太极 → 主界面内打开同步详情；功能页内点太极回到主界面。

### 卦位交互

- 每个卦位 ＝ 一个功能入口，样式为**卦象符号 + 卦名 + 五行强调色**（卦名随界面语言）。
- 被「玉兆管理」封印的功能 → 卦位呈**封印暗态**（灰化 + 角标，角标文案随语言）。
- 封印开关在「玉兆管理」页内切换（无长按/右键快捷封印）；长按悬浮入口 900ms 复位其位置。

### 各卦五行强调色

| 卦 | 色 | 卦 | 色 |
| --- | --- | --- | --- |
| 乾 | 金白 | 巽 | 青绿 |
| 兑 | 银泽 | 坎 | 玄蓝 |
| 离 | 赤红 | 艮 | 岩灰 |
| 震 | 苍碧 | 坤 | 土黄 |

## 四、八大功能详细设计

### 1. 交流讯息（兑）— 参考微信

- **联系人（一对一）**：道号/姓名、关系、最后传讯时间、未读数、消息列表。消息形态为"传音符"。
- **群聊（一对多）**：群名（如"青云宗·内门"）、成员数、消息含发送者。
- 数据行：`contact / msg / group / gmsg` 四类。

### 2. 天下论坛（巽）

- 所有持玉兆者可发帖；分**版块**（修炼心得 / 法器交流 / 悬赏 / 闲聊）。
- 帖子：作者、作者身份、标题、正文、"共鸣"（点赞）、评论列表。
- 数据行：`post / comment`。

### 3. 记事玉册（离）

- 备忘录：**分类（文件夹）+ 条目**；条目可加"禁制"（锁定）。
- 数据行：`folder / note`。

### 4. 交易坊市（震）

- **坊市行情**：在售物品（名、品阶、描述、价格、卖方）。
- **拍卖行**：物品、起拍价、当前价、剩余时间、出价人数。
- **订单**：物品、状态（待付款 / 已拍下 / 交易中 / 已完成 / 已取消）、价格、买/卖角色。
- 数据行：`listing / auction / order`。

### 5. 芥子空间（坎）

- **钱财**：种类（下品/中品/上品灵石、仙晶、贡献点…）+ 数额。
- **物品**：名、数量、品阶、描述。
- 数据行：`currency / item`。

### 6. 天下舆图（坤）

- **当前所在地**：地名、所属域、说明。
- **近期行踪**：时间、地点、动作/事件（轨迹）。
- 数据行：`current / track`。

### 7. 本命玉牌（乾）

- 分组字段：**基本**（名字/性别/身高/体重）、**仪容**（外貌/穿着）、**修为**（灵根/体质/境界/状态）、**隐秘**（隐秘信息）。
- 数据行：`field｜分组｜字段名｜值`。

### 8. 玉兆管理（艮）

- **本地系统页**，不由模型生成。
- 列出其余 7 个功能的**启用/封印**开关。
- 禁用后：该卦位封印、提示词不再请求该功能、不可打开。
- 这是**省 token 的核心机制**——只同步启用的功能。

## 五、数据协议设计

模型每轮输出一个 `<yz_jade>` 包裹块，内含 `<yz_meta>` + 各启用功能的子块。格式沿用"非 JSON、一行一条、`｜` 分字段"：

```
<yz_jade>
<yz_meta>
turn｜本轮唯一ID｜角色名｜本轮变化摘要｜full
</yz_meta>
<yz_msg>
contact｜id｜道号｜关系｜时间｜未读｜预览
msg｜联系人id｜消息id｜self或other｜时间｜内容
group｜群id｜群名｜成员数｜时间｜未读｜预览
gmsg｜群id｜消息id｜发送者｜self或other｜时间｜内容
</yz_msg>
<yz_forum>
post｜id｜作者｜身份｜版块｜时间｜标题｜正文｜共鸣数[｜player]
comment｜id｜作者｜时间｜内容
（post 行尾可选 owner：player = 玩家真实发帖，缺省 = 角色发帖）
</yz_forum>
<yz_notes>
folder｜id｜名称｜数量
note｜id｜文件夹id｜时间｜true或false｜标题｜正文
</yz_notes>
<yz_market>
listing｜id｜物品名｜品阶｜描述｜价格｜卖方
auction｜id｜物品名｜品阶｜描述｜起拍价｜当前价｜剩余时间｜出价人数
request｜id｜物品名｜品阶｜描述｜出价｜求购人
order｜id｜物品名｜状态｜价格｜时间｜买或卖
</yz_market>
<yz_space>
currency｜种类｜数额
item｜id｜名称｜数量｜品阶｜描述
</yz_space>
<yz_map>
current｜所在地｜所属域｜说明
track｜id｜时间｜地点｜动作
place｜id｜地点名｜所属域｜说明
</yz_map>
<yz_tablet>
field｜分组｜字段名｜值
</yz_tablet>
</yz_jade>
```

**校验约束**（仿 `assess`，逐功能判定"完整同步"）：

- 交流讯息：≥ 2 联系人且每人 ≥ 2 条消息，≥ 1 群且每群 ≥ 2 条消息（玩家传讯联系人豁免）
- 天下论坛：≥ 2 帖且每帖 ≥ 1 评论（玩家帖子豁免）
- 记事玉册：≥ 2 分类且 ≥ 3 条备忘
- 交易坊市：行情 / 求购 / 拍卖 / 订单四类均非空
- 芥子空间：钱财 + 至少 1 类物品
- 天下舆图：当前位置 + ≥ 2 条行踪 + ≥ 2 处地点名录
- 本命玉牌：六组字段齐全（基本/仪容/修为/功法/羁绊/隐秘，功法羁绊各 ≥1 条）

未达标 → 保留旧数据、标记 `partial`。

### 增量同步扩展（v2.0.0 → v2.1.0 diff 协议）

`<yz_meta>` turn 行第 5 字段取值：`full`（缺省同义，所有已启封分区整块完整输出）、
`part`（v2.0.0 仅含变化分区，v2.1.0 起仅作历史消息水化兼容保留）与 `diff`（v2.1.0
起常规轮次）。

**diff 轮**（git diff 语义）：分区内的行以 `+`/`-` 前缀输出——`+行` 为 upsert（id
已存在则整行替换，不存在则受分区上限约束追加；联系人/群行替换时保留其消息），`-行`
为删除（只给定位字段：id 行删 id、field 删组+键、currency 删种类、comment 删整行内容）；
未提及的行客户端原样保留。识别规则：meta 声明 `diff`，或任一分区出现 +/- 操作行（声明
缺失时以行形态为准——防止模型只输出变化行却被按 full 整块替换清掉未提及数据，这正是
v2.0.3 之前满屏红色感叹号与数据碎片化的根因）。无前缀行在 diff 分区内宽松按 `+` 处理。
没有任何变化时只输出 `<yz_meta>`（meta-only：状态保持、摘要更新、不动 revision）。

提示词始终注入 `<yz_current>` 当前数据基线（`buildCurrent(state, flags)` 把 state 按
协议行语法序列化，内部 `yzc_` 容器标签与输出格式同构但不会与 `<yz_jade>` 解析冲突；
封印功能不进；行数上限与解析器一致）。基线是模型唯一的数据事实来源（协议块已从历史
剥离，正文不含旧数据），diff 轮对照基线只输出变化行——输出 token 从「每轮全量数据」
降为「每轮变化行」。强制全量轮（状态 empty/revision===0，或任一封印切换后的第一次生成，
内存 `flagsDirty` 标记）仍输出完整区块并在成功后清除标记；diff/part 轮不清除。v2.0.0
的 `sync_scope` 设置（full/smart 二选一）已删除——diff 是唯一常规路径。

`sync.issues` 非空时提示词回显 ≤3 条未达标项（经 catalog 按 lang 翻译），要求模型用
`+ 行`补齐。剥离通道（TAG_PREFIX/stripBlocks/stripStreamTail/DOM 扫描）覆盖 `digest`
（历史残留）与 `current` 标签，防模型复读基线进正文。`lines()` 的项目符号剥离要求后随
空白（`^[-•*]\s+`），无空格前缀保留给 diff 操作行。

客户端评估矩阵：

| 场景 | 判定 |
| --- | --- |
| full | 与 v1.6 一致；skip 行视同分区缺失 |
| part 出现且达标 | 应用该分区；全部达标 → complete |
| part 出现不达标 | 丢弃保旧、partial、issue 来自本轮出现分区 |
| part skip 且旧数据达标 | 静默通过：不计 issue、不进 applied |
| part skip 但旧数据缺失 | 沿用现有 issue code、partial |
| part meta-only 且 revision>0 | 状态保持、摘要更新、不弹 Toast |
| part meta-only 且 revision===0 | invalid（防开局白卷） |
| diff 触及分区合并后达标 | 落盘合并结果、进 applied、revision+1 |
| diff 触及分区合并后不达标 | 不落盘保旧数据、记 issue（供 + 行修复回声） |
| diff 未触及分区 | 数据不动；重推导达标性（旧数据不达标 → issue 回显自愈，达标 → issue 清除） |
| diff meta-only 且 revision>0 | 状态保持、摘要更新、不动 revision |
| diff 全部不达标且 revision>0 | partial（旧数据仍在，区别于 full 的 invalid） |

### 注入预算与世界书归档（v2.1.0 token 上限）

diff 格式只降「输出」token；每轮注入的 `<yz_current>` 基线仍随数据增长（消息/笔记/帖子
上限全满时可达数万字符），多轮后 token 消耗滚雪球。v2.1.0 给注入侧加双保险：

- **条目级注入采样（v2.2.0）**：联系人/群聊/论坛帖子超过上限后（3/2/5），每轮只向
  基线注入采样子集——不再全量注入条目，彻底消除「条目数随对话增长、注入量线性膨胀」。
  `sampleEntries`（CORE 纯函数，rng 可注入）：**强制包含集**（必定注入、不参与概率）=
  `yz-player` 联系人、未读联系人、含 `pmg-*` 发言的群聊、`owner=player` 帖子与含
  `pmc-*` 评论或 `unread>0` 的帖子（玩家真实事件/未读新回复，消息行必须挂在可见条目下）；其余按**活跃度加权
  随机**补足到上限——`score = 1 + 消息数/评论数`（钳制 20，新增/冷门条目天然保底 1 分），
  `p_i ∝ score^γ`（γ=2）且钳制 `p_i ≥ β/n`（β=0.5，冷门与新增条目保留非零概率）。
  **隐藏条目完全不出现**（无 archived 行、无任何提示）：模型无记忆、只读上下文，
  不见即不知、不会去修改；diff 门禁兜底（隐藏条目不可引用）。每轮独立采样不持久化，
  活跃条目高分稳定连续出现。渲染/存储/评估（全量）/世界书归档不受影响——只裁剪
  注入视图。提示词明令：基线上未出现的条目仍存在，不得重建/删除/复制，也不得在
  叙事中宣称其消失。
- **最近窗口 + 归档摘要**：`buildCurrent` 每实体只序列化最近 `RECENT_MSG_ROWS=6` 条消息
   （联系人 20 条上限、群 24 条上限不变，超限由 `tail()` 保尾截断——最旧淘汰、最新保留，
   这是多轮后「群聊看不到最新消息」的修复）；笔记/挂单/拍卖/求购/地点/物品窗口为
   3/6/6/6/6/10，超窗条目以 `archived｜类型｜id｜摘要` 行出现（该行类型不在任何解析器
   白名单里，模型复读也不会污染解析）。帖子级窗口与帖子 archived 行已由条目级采样
   取代（采样选中的帖子全行注入，评论窗口仍为 6）。提示词规则：归档条目不要复述、不要用 + 行整行替换
  （会丢正文），只能 - 行删除。所有行的时间字段一律使用绝对日期（如 丙午年五月十二 午时），
  禁止 今日/昨日 等相对表述——消息进入世界书归档后可能在剧情日期更晚时被召回，相对时间
  会误导模型。
- **帖子未读机制（v2.2.0）**：`post` 行第 10 字段 `unread` = 新回复数（模型维护：
  有新评论时 +1，已读处理回复后以 `+post` 显式清零；diff 未显式带 unread 时保留
  原值防误清零；旧格式第 10 字段为 owner 时启发式兼容 unread=0；玩家帖因门禁
  保护 unread 恒 0）。unread>0 的帖子采样强制包含，列表置顶 + 呼吸光效
  （与联系人/群聊一致）。
- **全局字符上限**：`MAX_BASELINE_CHARS=9000`——所有行计入预算，超限按五级淘汰：
  ① 其它功能的明细行（消息/笔记/帖子正文，消息在世界书有召回）→ ② tablet 字段行
  （角色设定排最后）→ ③ 归档摘要行 → ④ last 标记行（玩家传讯未读行、玩家帖子与其
  评论，真实事件，仅极端场景让位）→ ⑤ 超长标识行截断到 `ROW_HARD_CAP=160`（保留行首
  id/name 供 diff 定位，丢 preview/time 等长尾）。实体标识行（contact/group/folder/
  order/currency/track 等，diff 定位与存在性依赖）永不整行淘汰。数据再大每轮注入量被
  控制在硬上限内，不再随轮次滚雪球。
- **世界书召回（相关联的数据）**：窗口外的历史消息镜像到插件管理的世界书
  （`tavo.lorebook`，书名为 `玉兆档案·<chatId>`），每个联系人/群一个关键词条目
  （keywords=实体名、scanDepth=4、atDepth 注入、system 角色、内容 ≤6000 字符、只读
  声明脚注）。正文提及实体名时 Tavo 才注入其完整归档——召回量=本轮相关实体，而非全部。
  归档与基线窗口共用 `RECENT_MSG_ROWS` 切分，两通道不重叠不遗漏。地点名录另有专属
  「玉兆·地点名录」关键词条目：关键词 = 窗口外归档地点名，正文提及地点名时注入完整
  名录（与基线窗口共用 `RECENT_PLACE_ROWS` 切分；封印舆图后条目移除）。同步时机：
  msg 分区落盘后的后台刷新（generation:success / message 钩子 / 历史重建 / 管理页清空
  与导入），带 busy+pending 防重入；挂接聊天用 `tavo.chat.current()` 读既有世界书
  列表后合并（chat.update 整体替换，不覆盖用户自己的世界书），且只在目标即当前聊天时
  挂接。世界书能力缺失/API 失败一律静默降级（基线窗口照常工作，无报错面）。

### 同步诊断与卦位徽标（v2.0.0 可观测层）

- `renderSyncDetail(state)` 单一渲染器三入口共用：主界面 `.yz-sync` 行按钮、太极核心
  按钮（功能页内回主界面、主界面内开详情）、管理页顶部折叠诊断区。展示状态徽标/摘要/
  turnId+lastSource/applied 分区/issues 翻译/lastError（非空才显示）/更新时间（手工
  `YYYY-MM-DD HH:mm` UTC 格式，不用 toLocaleString）/容量进度条（>80% 警示色）/
  revision/processedTurns 数/chatId。
- 管理页数据操作：单功能清空（行尾次级按钮两击确认，5 秒超时还原，纯函数
  `nextWipeState` 直测；执行 = 所有空间的该分区重置 blank + 落盘，不动 processedTurns）、
  存档导出（只读 textarea + 全选复制；导出内容超过导入容量红线
  `MAX_SNAPSHOT_BYTES` 时拦截并提示，保证导出→导入 round-trip 不断裂）、导入
  （normalizeState 校验 + 超 MAX_SNAPSHOT_BYTES 拒收）。
- 卦位三态徽标互斥优先级警示 > 未读 > 新：警示取 `sync.issues` path 前缀映射功能；未读
  为 contacts+groups unread 总和（仅兑卦，>99 显示 `99+`）；新为 `sync.applied` 含该功能
  且不在 `sync.appliedSeen`（持久化于 state.sync，openFeature 并入、下轮 applied 移除），
  仅呼吸光效不加文字角标。封印态不显示任何徽标；aria-label 附徽标语义。
- 侧边栏动作：`open-jade`（复用 open()）、`resync-history`（rebuildFromHistory + toast）。
  重建 = 从世界书快照（权威存储）重新装载整份状态（含全部用户空间）并覆盖内存态；
  快照/镜像均无数据时保留现状并提示「世界书中没有玉兆数据快照」。
  历史消息正文经正文剥离后不含协议块，不再作为重建数据源。

## 六、用户空间架构（v3.0.0 改版）

双域设计（角色域/玩家域）废除。每份聊天的数据由 N 个**用户空间**承载，
只维护一套 UI；跨域镜像通道（syncPlayerChannel/Groups/Posts、已读游标、
投递重试、公开/私有数据区分）整体移除。

### 6.1 数据模型

- 顶层 `state`：`{schemaVersion:2, chatId, pluginVersion, pendingFull, activeSpaceId,
  migratedPlayer, hydration, spaces:[Space], updatedAt}`。单存储键、单快照链。
- `Space`：与旧版状态同构的全套分区（tablet/chats/notes/forum/market/space/map）
  加空间元信息与模型域字段：
  `{id, name, isDefault, sendToAI, allowAIWrite, createdAt, revision, processedTurns,
  sync, updatedAt}`。每个空间有自己的 revision/sync/去重指纹。
- **默认空间**（id `sp0`）：锚定 `{{char}}`（代指角色）。name 恒空，显示时解析为
  角色名（`sync.roleName` 优先）；`allowAIWrite` 恒真（协议缺省落点）；可删除，
  无空间参数的数据到达时自动重建。
- 自定义空间：`createSpace(name)` 上限 6 个、名称唯一（trim + 大小写不敏感）；
  可改名；两 flag 默认开。归一层兜底：重复 id 重发、重名补后缀、多默认只留第一个。
- v1→v2 迁移在 `normalizeState` 内完成（顶层分区整体搬进默认空间，读到时即升级，
  无迁移窗口）；旧玩家域数据在 `migratePlayerToSpace`（doSwitchChat 链路）一次
  性升级为「我」空间并删除旧键，`migratedPlayer` 标记防重复迁移。

### 6.2 AI 协议：空间参数路由

- `turn` 行第 6 字段（可选）= 目标空间名；缺省/`sp0` → 默认空间。
  `findSpaceState` 按 id → 名称（trim+忽略大小写）解析。
- 拒写记入目标（或默认）空间 `sync.issues` 并回声：`space.unknown`（未知空间）、
  `space.denied`（allowAIWrite=false）、`space.full`（非默认空间只接受 diff 轮——
  全量轮会抹掉用户私有数据，直接丢弃）。
- 基线注入按空间分组：`sendToAI` 的空间各出 `<yzc_*>` 容器，默认空间不带属性、
  自定义空间带 `space="空间名"`；条目采样上限按发送空间数均摊（保底 1），
  五级预算淘汰全空间共用一条池子。完整性底线评估（assess）只对默认空间生效。

### 6.3 真实发言门禁（任何空间一致）

- 用户内容 id 前缀：联系人 `c-<n>`、私讯 `pm-<n>`、群讯 `pmg-<n>`、评论 `pmc-<n>`、
  用户帖 `owner=player`。模型不得增删改这些行（diff 应用器直接丢弃）；
  c- 线程允许模型追加新行（self/other 皆可，不得覆盖/删除既有行）。
- 未读为客户端语义：用户线程 unread = 最后一条 pm/pmg 之后的回复数 − `seen`
  （打开详情 `markSpaceThreadSeen` 推进）；用户帖 unread = 非用户评论数 − `seen`。
  `recomputeThreadUnread` 在每次空间写入与快照应用后重算；模型线程的 unread
  仍由协议字段维护，不被触碰。

### 6.4 UI：一套空间视图

- 顶栏空间按钮显示当前空间名，点击进入管理页 spaces 视图：切换（进入）、
  发送AI / AI可写开关、改名（默认空间禁改）、删除（两击确认 + 6s 撤销，
  默认空间按钮文案「清空重建」）、底部新建输入。
- `activeSpaceId` 持久化在 state（每聊天独立）；空间切换做子页消毒（详情/表单
  回退根视图）。所有功能页统一可编辑：讯息列表带「新增联系人」CTA（自定义名称），
  私聊/群聊/论坛评论在任何空间都有发言输入框，玉册/坊市订单/储物/舆图删除、
  表单编辑（entity-new/edit/save/delete 动作）不再是玩家域专属。
- 气泡左右：右 = 用户真实发言（pm/pmg id）；默认空间的模型线程维持旧语义
  （self 在右）；用户空间/用户线程里 AI 一律在左带发送者名。
- 空间数据变更统一走 `saveSpace`（updatedAt + 未读重算 + 落盘）。
- 归档/召回条目只从默认空间构建（用户空间数据每轮基线注入，无需归档）。

## 七、状态与持久化

- 按 `chatId` 隔离；`chat:opened` 切换加载、重建按钮/`message:deleted` 从世界书快照恢复。
- `processedTurns` 去重，防同一轮重复应用；重复投递的轮次不重复落盘。
- 快照超阈值（200KB）拒收；解析失败转 Toast 并保留旧数据。
- **世界书为主存储（v2.2.0 持久化改造）**：权威数据在世界书 `玉兆档案·<chatId>` 的
  分片快照条目（角色域 `yz-snap-N` / 玩家域 `yz-psnap-N`，enabled:false 永不注入，
  每片 ≤90KB、单域 ≤5 片，包装 `{v, ver, rev, updatedAt, kind, index, total, body}`，
  读取按 index 拼接还原；兼容旧版单条 `yz-snap`）。本地镜像（`yz-jade-v1:<chatId>` /
  `yz-jade-player-v1:<chatId>`）只是启动加速缓存，可随时丢弃。存储回退链 = 世界书
  （权威）→ 本地镜像（缓存）→ 空白。save 只在"有实际数据"时排队同步（空白/未加载
  状态不得覆盖已有书）；syncArchive 无可写内容时不触碰已有书。宿主 chat 键与全局
  备份已移除（不再有切聊复查竞态）；UI 偏好（封印开关/FAB 位置）仍走 global 键。
- **镜像/世界书 tie-break**：save 先写镜像（同步）后排队写世界书（async busy 合并），
  因此镜像的 updatedAt 恒不早于世界书；load 时 revision 严格更高或 revision 平局且
  updatedAt 更新者胜——rev-0 聊天与游标/未读等 revision 中性变更不会被陈旧世界书
  覆盖，陈旧世界书由镜像治愈回写。
- **并发写安全**：镜像同步在 await 后复查状态对象同一性（双通道交错不覆盖新轮次）；
  hydrateHistory 写回前引用复查（水化窗口内新轮次不被静默回滚）；落盘队列与 load
  协同（切聊前先排空队列，rev-0 首条玩家消息不再丢失）。
- **版本迁移（v2.1.0 防旧数据粘滞）**：状态记录 `pluginVersion`；加载/导入时发现版本
  变化即置持久化强制全量标记 `state.pendingFull`（封印切换同样置位，替代内存
  flagsDirty 重启丢失的盲区）。下一轮生成按新提示词强制全量重写全部数据（旧格式行、
  相对时间等一次性刷新），成功应用一轮完整全量（`applySnapshot` 中非 diff/part 且
  assessment.ok）后清除标记；diff/part 轮不清除。全量轮提示词含「时间字段为相对表述
  的行本轮一律改写为绝对日期」规则——否则「基线数据视为既定事实」会诱导模型把旧相对
  时间原样照抄进全量输出，粘滞修复被规则自相矛盾抵消。
- **旧数据兼容**：全部历史版本 schemaVersion 恒为 1 且字段集是当前字段集的子集，
  normalizeState 白名单重建双向吸收（缺失补空、畸形消毒、超限截断），结构上不存在
  不兼容路径；若未来发生结构性变更，以 schemaVersion 显式迁移 + pendingFull 全量重建
  兜底，而不是原地改写旧档。
- 双写规则：save 先写本地镜像再写宿主键（宿主键带当前聊天复查，切走时跳过宿主写入，镜像与全局备份照常）；load 以 revision 或 updatedAt 更新的数据源为准——镜像更新时以镜像为准（宿主键陈旧则被治愈回写），host 仅作镜像缺失兜底。

## 八、视觉风格

- **玉质**：青玉/白玉透光、温润反光。
- **纹饰**：祥云纹、雷纹、篆书铭文环绕玉盘。
- **光效**：灵气流光、卦位呼吸光晕、太极缓转。
- **字体**：标题篆/隶，正文保证可读。
- 悬浮入口为一枚**玉佩/玉简**（可拖拽），点击展开太极八卦盘。

## 九、设置项（settings.schema）

| key | 类型 | 说明 |
| --- | --- | --- |
| `enabled` | switch | 启用玉兆 |
| `auto_strip` | switch | 自动隐藏正文中的玉兆数据块 |
| `lang` | select | **提示词语言**（中/EN）——决定注入提示词与生成内容的语言 |

界面语言不设开关：跟随宿主 App 语言（`tavo.plugin.i18n`），语言切换时全量重渲染。

## 十、关键实现决策

| 决策 | 说明 |
| --- | --- |
| 全屏 UI 直接挂载，不走 `htmlFragments` | 法器是全视口模态 + 全局悬浮入口，需在 Hook 期间独立于聊天页生命周期工作；宿主没有「当前是否聊天页」的查询 API，用 `chatActive`（chat:opened/chat:closed 维护）门控 FAB 显隐，配合禁用收起并隐藏 FAB、`visibilitychange` / window focus 刷新 |
| z-index 取单档 `Z_INDEX_TOP = 2147483646` | overlay 与 FAB 共用同一常量，全屏模态语义需要顶层；刻意避开 2147483647 最大值，给与宿主或其它插件的层级协调留一档余量 |
| FAB 默认位置 `bottom:96px; right:16px` | 由 `FAB_MARGIN_BOTTOM/FAB_MARGIN_RIGHT` 常量统一供 CSS 初始位置与长按复位/管理页复位按钮使用，保证「复位」即回到初始位置；真机（手机/平板/桌面）仍需逐一验证默认与复位位置不遮挡发送等控件 |
| generation:success 内部顺序不变式 | 读 payload → 同步剥离事件正文 → 异步应用快照 → 后台落盘。该 Hook 每个 handler 只有约 5 秒预算且超时整体丢弃：剥离是阻止协议块进入已保存消息的关键动作，纯字符串操作，必须在任何 await 之前完成 |
| 持久化为后台串行队列 | 内存态同步更新，写盘不阻塞调用方（同 key 写入按入队顺序生效）；重复投递轮次不再触发落盘；序列化体积超限仅在 yz_debug 下告警 |
| 正文剥离双通道 | 权威路径在 Hook 层：generation:success 同步剥离事件正文、message hook 应用快照；兜底路径：MutationObserver 收集增量变更记录（220ms 防抖、队列上限），只扫描新增/变化的文本节点，启动时做一次全量扫描；两条路径都受 `enabled()` + `auto_strip` 门控 |
| 水化版本标记 | `state.hydration.sig = 消息条数:末条id`，未变化时跳过历史全文扫描。已知盲区：编辑中间楼层不改变签名，由「message:updated 不带信封 → 去抖 `rebuildFromHistory`」兜底；消息删除同样走快照恢复 |
| 内存聊天缓存上限 | 按 chatId LRU 只保留最近 5 个会话的内存态；淘汰无损——每次写入已落盘，重进时从宿主/本地重新加载 |
| 历史水化只查 assistant | role 过滤枚举按 API 文档只有 system/assistant/user；群聊多角色以 `role:'assistant'` + 不同 characterId 表达，不做别名兜底查询 |
| 宿主行为依赖清单（每次升级 Tavo 后回归） | ① entry 可访问顶层 window/document 并向 body 挂载（跨域 iframe 时回退当前文档）；② Pointer Events、MutationObserver、TreeWalker 可用；③ `tavo.get/set` 与 localStorage 可用。全屏 UI 与剥离通道依赖以上行为，宿主若提供页面级挂载点应迁移并删除手工门控 |
| issues 只存 `{path, code}` | 文案在 catalog（`assess.issue.*`），展示时按界面语言翻译 |

## 十一、变更记录

版本变更记录统一保存在 [CHANGELOG.md](CHANGELOG.md)（最新版本在最前）。
